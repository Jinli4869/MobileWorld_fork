---
phase: 07-conformance-artifact-contract-closure
plan: 03
type: execute
wave: 3
depends_on: [07-01, 07-02]
files_modified:
  - tests/protocol/test_phase7_conformance_artifact_contract.py
autonomous: true
requirements: [COMP-02, TRCE-01]
user_setup: []
must_haves:
  truths:
    - "Runtime-generated artifacts pass conformance suite checks for canonical header and policy manifest."
    - "Conformance suite fails deterministically when required artifact fields are removed."
    - "`benchmark conformance` CLI path can evaluate runtime-generated artifacts and emit PASS report output."
  artifacts:
    - path: tests/protocol/test_phase7_conformance_artifact_contract.py
      provides: "Runtime-to-conformance regression tests covering positive and negative contract cases."
  key_links:
    - from: tests/protocol/test_phase7_conformance_artifact_contract.py
      to: src/mobile_world/runtime/protocol/conformance.py
      via: run_conformance_suite assertions on check names
      pattern: "canonical\\.header_present|meta\\.policy_manifest_present"
    - from: tests/protocol/test_phase7_conformance_artifact_contract.py
      to: src/mobile_world/core/subcommands/benchmark.py
      via: benchmark conformance command execution
      pattern: "benchmark.*conformance"
---

<objective>
Add end-to-end conformance regression coverage that protects the eval-artifact-to-conformance workflow against relapse.

Purpose: Close COMP-02 and TRCE-01 with runtime-generated artifact validation, not synthetic-only fixtures.
Output: Dedicated Phase 7 protocol test module covering positive path, failure path, and benchmark conformance CLI invocation.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-conformance-artifact-contract-closure/07-RESEARCH.md
@.planning/phases/07-conformance-artifact-contract-closure/07-PATTERNS.md
@.planning/phases/07-conformance-artifact-contract-closure/07-VALIDATION.md
@src/mobile_world/runtime/protocol/conformance.py
@src/mobile_world/core/subcommands/benchmark.py
@src/mobile_world/core/cli.py
@src/mobile_world/core/runner.py
@src/mobile_world/runtime/utils/trajectory_logger.py
@tests/protocol/test_phase4_nanobot_reference_integration.py
@tests/protocol/test_phase6_reporting_conformance_reproducibility.py

<interfaces>
From `src/mobile_world/runtime/protocol/conformance.py`:
- `def run_conformance_suite(log_root: str) -> dict[str, Any]`
- task checks include `canonical.header_present` and `meta.policy_manifest_present`

From `src/mobile_world/core/subcommands/benchmark.py`:
- `async def execute(args: argparse.Namespace) -> None` with `benchmark conformance` branch

From `src/mobile_world/core/runner.py`:
- `def _execute_single_task(...) -> tuple[int, float]` runtime artifact generation path used in protocol tests
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create runtime artifact conformance regression module</name>
  <files>tests/protocol/test_phase7_conformance_artifact_contract.py</files>
  <read_first>
    - tests/protocol/test_phase4_nanobot_reference_integration.py
    - tests/protocol/test_phase6_reporting_conformance_reproducibility.py
    - src/mobile_world/runtime/protocol/conformance.py
    - src/mobile_world/runtime/utils/trajectory_logger.py
  </read_first>
  <action>Create `tests/protocol/test_phase7_conformance_artifact_contract.py` with helper(s) that generate artifacts via runtime paths (TrajLogger + task execution helpers), then add `test_runtime_artifacts_pass_conformance` asserting `run_conformance_suite(...)[\"ok\"] is True` and explicit PASS for `canonical.header_present` + `meta.policy_manifest_present` checks.</action>
  <acceptance_criteria>
    - `rg -n "test_runtime_artifacts_pass_conformance|run_conformance_suite" tests/protocol/test_phase7_conformance_artifact_contract.py` returns matches.
    - Positive-path test asserts PASS for check names `canonical.header_present` and `meta.policy_manifest_present`.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_phase7_conformance_artifact_contract.py::test_runtime_artifacts_pass_conformance</automated>
  </verify>
  <done>Runtime-generated artifact bundles are validated as conformance-pass through the same conformance suite used by CLI.</done>
</task>

<task type="auto">
  <name>Task 2: Add negative-path conformance checks for missing header/policy fields</name>
  <files>tests/protocol/test_phase7_conformance_artifact_contract.py</files>
  <read_first>
    - tests/protocol/test_phase7_conformance_artifact_contract.py
    - src/mobile_world/runtime/protocol/conformance.py
    - .planning/v1.0-v1.0-MILESTONE-AUDIT.md
  </read_first>
  <action>Add regression tests that mutate generated artifacts to remove (a) canonical header event and (b) `policy_manifest`, then assert conformance fails with the exact failing checks (`canonical.header_present` and `meta.policy_manifest_present`) to lock blocker-gap behavior.</action>
  <acceptance_criteria>
    - `rg -n "canonical\\.header_present|meta\\.policy_manifest_present" tests/protocol/test_phase7_conformance_artifact_contract.py` returns failing-check assertions.
    - At least one negative-path test asserts `report[\"ok\"] is False`.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_phase7_conformance_artifact_contract.py</automated>
  </verify>
  <done>Conformance regressions now fail fast when either required contract field is absent.</done>
</task>

<task type="auto">
  <name>Task 3: Verify benchmark conformance CLI path against runtime-generated artifacts</name>
  <files>tests/protocol/test_phase7_conformance_artifact_contract.py</files>
  <read_first>
    - src/mobile_world/core/cli.py
    - src/mobile_world/core/subcommands/benchmark.py
    - tests/protocol/test_phase6_reporting_conformance_reproducibility.py
  </read_first>
  <action>Add a CLI-path regression that parses `benchmark conformance --log-root ... --output ...` and executes the benchmark subcommand handler against runtime-generated artifacts; assert output report file exists and contains `ok: true` for the positive fixture.</action>
  <acceptance_criteria>
    - `rg -n "benchmark\"\\s*,\\s*\"conformance|--log-root|--output" tests/protocol/test_phase7_conformance_artifact_contract.py` returns CLI invocation coverage.
    - Test asserts generated report JSON exists and `report[\"ok\"] is True`.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_phase7_conformance_artifact_contract.py</automated>
  </verify>
  <done>Eval-artifact-to-conformance command path is regression-protected with runtime-produced artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime artifact generator -> conformance suite | Test harness consumes filesystem artifacts that may be incomplete/corrupted |
| CLI argument parser -> benchmark conformance executor | User-provided `--log-root` input drives artifact traversal and validation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03-01 | T | conformance critical artifact fields | mitigate | Add positive/negative regression tests that assert exact check names for header and policy manifest presence |
| T-07-03-02 | D | conformance CLI/reporting workflow | mitigate | Execute CLI-path regression on runtime-generated artifacts and assert deterministic PASS/FAIL outputs |
| T-07-03-03 | I | log-root file traversal | accept | Existing conformance implementation restricts to task directories and fixed filenames; no new path handling logic added in this phase |
</threat_model>

<verification>
Nyquist-aligned automated checks for this plan:
- `UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_phase7_conformance_artifact_contract.py`
- `UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_canonical_trajectory_contract.py tests/protocol/test_phase7_conformance_artifact_contract.py`
- `UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol`
</verification>

<success_criteria>
- New Phase 7 regression module validates runtime-produced artifacts against conformance suite.
- Tests explicitly enforce PASS/FAIL behavior for `canonical.header_present` and `meta.policy_manifest_present`.
- Benchmark conformance CLI branch is exercised in automated tests with runtime-generated artifacts.
</success_criteria>

<output>
After completion, create `.planning/phases/07-conformance-artifact-contract-closure/07-03-SUMMARY.md`
</output>
