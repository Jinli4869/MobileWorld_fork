---
phase: 07-conformance-artifact-contract-closure
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/mobile_world/runtime/utils/trajectory_logger.py
  - src/mobile_world/core/runner.py
  - tests/protocol/test_phase2_tool_router_policy.py
autonomous: true
requirements: [COMP-02]
user_setup: []
must_haves:
  truths:
    - "Runtime canonical metadata includes `policy_manifest` for conformance artifact checks."
    - "Policy manifest content comes from `CapabilityDecision.as_manifest()` and is serialized consistently."
    - "Policy manifest is persisted before task execution so early task failures still produce required metadata."
  artifacts:
    - path: src/mobile_world/runtime/utils/trajectory_logger.py
      provides: "Dedicated policy manifest persistence API for legacy + canonical metadata."
    - path: src/mobile_world/core/runner.py
      provides: "Runner call path that writes policy manifest from resolved capability decision."
    - path: tests/protocol/test_phase2_tool_router_policy.py
      provides: "Regression coverage for tool/policy manifest artifact persistence."
  key_links:
    - from: src/mobile_world/core/runner.py
      to: src/mobile_world/runtime/protocol/capability_policy.py
      via: resolve_capability_policy + as_manifest
      pattern: "capability_decision\\.as_manifest\\("
    - from: src/mobile_world/core/runner.py
      to: src/mobile_world/runtime/utils/trajectory_logger.py
      via: log_policy_manifest
      pattern: "log_policy_manifest"
---

<objective>
Close the conformance metadata gap by persisting `policy_manifest` in runtime artifact output.

Purpose: Satisfy `meta.policy_manifest_present` checks in conformance validation for eval-produced artifacts.
Output: Runner/logger plumbing that writes policy manifest deterministically plus focused regression coverage.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-conformance-artifact-contract-closure/07-RESEARCH.md
@.planning/phases/07-conformance-artifact-contract-closure/07-PATTERNS.md
@src/mobile_world/core/runner.py
@src/mobile_world/runtime/protocol/capability_policy.py
@src/mobile_world/runtime/utils/trajectory_logger.py
@src/mobile_world/runtime/protocol/conformance.py
@tests/protocol/test_phase2_tool_router_policy.py

<interfaces>
From `src/mobile_world/runtime/protocol/capability_policy.py`:
- `def resolve_capability_policy(...) -> CapabilityDecision`
- `CapabilityDecision.as_manifest() -> dict`

From `src/mobile_world/runtime/utils/trajectory_logger.py`:
- `def log_tool_manifest(self, manifest: dict) -> None`

From `src/mobile_world/runtime/protocol/conformance.py`:
- `add_check("meta.policy_manifest_present", "policy_manifest" in meta)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add policy manifest artifact writer to TrajLogger</name>
  <files>src/mobile_world/runtime/utils/trajectory_logger.py</files>
  <read_first>
    - src/mobile_world/runtime/utils/trajectory_logger.py
    - src/mobile_world/runtime/protocol/conformance.py
    - src/mobile_world/runtime/protocol/capability_policy.py
  </read_first>
  <action>Add `log_policy_manifest(self, manifest: dict) -> None` that mirrors `log_tool_manifest` behavior by writing `policy_manifest` to both `traj.json` (`legacy["0"]["policy_manifest"]`) and `traj.meta.json` (`meta["policy_manifest"]`) with stable JSON serialization.</action>
  <acceptance_criteria>
    - `rg -n "def log_policy_manifest" src/mobile_world/runtime/utils/trajectory_logger.py` returns exactly one method definition.
    - `rg -n "policy_manifest" src/mobile_world/runtime/utils/trajectory_logger.py` shows writes to both legacy and canonical metadata paths.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_phase2_tool_router_policy.py::test_tool_manifest_and_error_logged_to_artifacts</automated>
  </verify>
  <done>Logger exposes explicit policy manifest persistence used by runner and tests.</done>
</task>

<task type="auto">
  <name>Task 2: Persist policy manifest in runner capability-resolution path</name>
  <files>src/mobile_world/core/runner.py</files>
  <read_first>
    - src/mobile_world/core/runner.py
    - src/mobile_world/runtime/protocol/capability_policy.py
    - src/mobile_world/runtime/utils/trajectory_logger.py
  </read_first>
  <action>After `resolve_capability_policy(...)`, compute `capability_manifest = capability_decision.as_manifest()` once, then call `traj_logger.log_tool_manifest(capability_manifest)` and `traj_logger.log_policy_manifest(capability_manifest)` before task execution starts. Keep behavior identical for MCP-enabled and MCP-disabled runs (no conditional skip for policy manifest).</action>
  <acceptance_criteria>
    - `rg -n "capability_manifest\\s*=\\s*capability_decision\\.as_manifest\\(" src/mobile_world/core/runner.py` returns one match.
    - `rg -n "log_tool_manifest\\(capability_manifest\\)|log_policy_manifest\\(capability_manifest\\)" src/mobile_world/core/runner.py` returns two matches.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_phase2_tool_router_policy.py::test_tool_manifest_and_error_logged_to_artifacts</automated>
  </verify>
  <done>Runner writes deterministic policy manifest metadata for every task execution path.</done>
</task>

<task type="auto">
  <name>Task 3: Add regression assertions for policy manifest artifact persistence</name>
  <files>tests/protocol/test_phase2_tool_router_policy.py</files>
  <read_first>
    - tests/protocol/test_phase2_tool_router_policy.py
    - src/mobile_world/runtime/utils/trajectory_logger.py
    - src/mobile_world/runtime/protocol/conformance.py
  </read_first>
  <action>Extend the artifact persistence test to call `log_policy_manifest(...)` and assert `policy_manifest` fields are present in both legacy and canonical metadata artifacts while preserving existing tool error assertions. Rename test to `test_tool_and_policy_manifest_and_error_logged_to_artifacts` for unambiguous intent.</action>
  <acceptance_criteria>
    - `rg -n "test_tool_and_policy_manifest_and_error_logged_to_artifacts" tests/protocol/test_phase2_tool_router_policy.py` returns one test.
    - `rg -n "policy_manifest" tests/protocol/test_phase2_tool_router_policy.py` shows assertions against both legacy and canonical metadata payloads.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_phase2_tool_router_policy.py</automated>
  </verify>
  <done>Protocol regression fails if policy manifest is not persisted in runtime artifacts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| capability resolver -> runtime logger | Policy decisions cross from in-memory decision objects into persisted artifacts |
| runtime artifacts -> conformance suite | Conformance verdict depends on immutable manifest fields in canonical metadata |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02-01 | T | runner capability manifest serialization | mitigate | Serialize `policy_manifest` from `CapabilityDecision.as_manifest()` only; do not hand-roll policy payloads |
| T-07-02-02 | R | artifact audit trail (`traj.meta.json`) | mitigate | Persist policy manifest before task execution and enforce with regression assertions in protocol tests |
| T-07-02-03 | D | conformance gate reliability | mitigate | Keep deterministic required-field persistence to prevent false negatives from missing metadata |
</threat_model>

<verification>
Run:
- `UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_phase2_tool_router_policy.py`
- `UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_canonical_trajectory_contract.py`
</verification>

<success_criteria>
- `traj.meta.json` written through runtime path contains `policy_manifest` and `tool_manifest`.
- `tests/protocol/test_phase2_tool_router_policy.py` enforces policy manifest persistence in both legacy and canonical artifacts.
- Runner writes policy manifest from a single `capability_decision.as_manifest()` source.
</success_criteria>

<output>
After completion, create `.planning/phases/07-conformance-artifact-contract-closure/07-02-SUMMARY.md`
</output>
