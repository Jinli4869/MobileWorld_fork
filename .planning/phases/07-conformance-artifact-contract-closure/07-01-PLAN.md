---
phase: 07-conformance-artifact-contract-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mobile_world/runtime/utils/trajectory_logger.py
  - tests/protocol/test_canonical_trajectory_contract.py
autonomous: true
requirements: [TRCE-01]
user_setup: []
must_haves:
  truths:
    - "Runtime-generated canonical JSONL contains a canonical header event for each task artifact bundle."
    - "Header emission is idempotent (exactly one header event in the canonical JSONL stream)."
  artifacts:
    - path: src/mobile_world/runtime/utils/trajectory_logger.py
      provides: "Emit-once canonical header insertion into runtime JSONL artifacts."
    - path: tests/protocol/test_canonical_trajectory_contract.py
      provides: "Regression checks for canonical header presence/count/schema fields."
  key_links:
    - from: src/mobile_world/runtime/utils/trajectory_logger.py
      to: src/mobile_world/runtime/protocol/events.py
      via: CanonicalTrajectoryHeader model serialization
      pattern: "CanonicalTrajectoryHeader\\("
    - from: tests/protocol/test_canonical_trajectory_contract.py
      to: src/mobile_world/runtime/utils/trajectory_logger.py
      via: TrajLogger runtime artifact assertions
      pattern: "header_events|schema_version"
---

<objective>
Close TRCE-01 canonical artifact drift by ensuring runtime JSONL output always contains one canonical header event.

Purpose: Keep runtime-produced trajectories compliant with canonical schema expectations used by conformance validation.
Output: Header emission guard in `TrajLogger` plus regression assertions that fail on missing or duplicated headers.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-conformance-artifact-contract-closure/07-RESEARCH.md
@.planning/phases/07-conformance-artifact-contract-closure/07-PATTERNS.md
@.planning/phases/07-conformance-artifact-contract-closure/07-VALIDATION.md
@src/mobile_world/runtime/utils/trajectory_logger.py
@src/mobile_world/runtime/protocol/events.py
@src/mobile_world/runtime/protocol/conformance.py
@tests/protocol/test_canonical_trajectory_contract.py

<interfaces>
From `src/mobile_world/runtime/utils/trajectory_logger.py`:
- `def log_traj(...) -> None`
- `def log_score(...) -> None`
- `def log_metrics_summary(...) -> None`
- `def _append_canonical_event(self, event: dict) -> None`

From `src/mobile_world/runtime/protocol/events.py`:
- `class CanonicalTrajectoryHeader(BaseModel)` with `type="header"` and canonical `schema_version`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add emit-once canonical header insertion in runtime logger</name>
  <files>src/mobile_world/runtime/utils/trajectory_logger.py</files>
  <read_first>
    - src/mobile_world/runtime/utils/trajectory_logger.py
    - src/mobile_world/runtime/protocol/events.py
    - src/mobile_world/runtime/protocol/conformance.py
  </read_first>
  <action>Implement a private header-emission guard in `TrajLogger` that writes one canonical `header` event to `traj.canonical.jsonl` using `CanonicalTrajectoryHeader` (no ad-hoc dict). Call the guard before appending canonical events in `log_traj`, and also in non-step event writers (`log_score`, `log_metrics_summary`) so runs that emit score/metrics first still include a header event.</action>
  <acceptance_criteria>
    - `rg -n "def _ensure_canonical_header_event|CanonicalTrajectoryHeader\\(" src/mobile_world/runtime/utils/trajectory_logger.py` returns matches.
    - `rg -n "_ensure_canonical_header_event\\(" src/mobile_world/runtime/utils/trajectory_logger.py` shows call sites in `log_traj`, `log_score`, and `log_metrics_summary`.
    - Header emission path appends at most one `\"type\": \"header\"` event per task log.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "def _ensure_canonical_header_event|CanonicalTrajectoryHeader\\(" src/mobile_world/runtime/utils/trajectory_logger.py && rg -n "_ensure_canonical_header_event\\(" src/mobile_world/runtime/utils/trajectory_logger.py'</automated>
  </verify>
  <done>Runtime logger guarantees canonical JSONL header presence with idempotent emission behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Strengthen canonical contract test for header presence and uniqueness</name>
  <files>tests/protocol/test_canonical_trajectory_contract.py</files>
  <read_first>
    - tests/protocol/test_canonical_trajectory_contract.py
    - src/mobile_world/runtime/protocol/conformance.py
    - .planning/phases/07-conformance-artifact-contract-closure/07-RESEARCH.md
  </read_first>
  <action>Update `test_trajectory_logger_writes_legacy_and_canonical_artifacts` to assert canonical JSONL contains exactly one header event with schema version `1.0.0`, correct task identity, and that step/score events are still present and schema-valid.</action>
  <acceptance_criteria>
    - `rg -n "header_events|len\\(header_events\\) == 1|header_event\\[\"schema_version\"\\]" tests/protocol/test_canonical_trajectory_contract.py` returns matches.
    - Test asserts both `step` and `score` events continue to exist after header insertion.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_canonical_trajectory_contract.py</automated>
  </verify>
  <done>Contract regression test fails on missing/duplicate header and passes on canonical runtime output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runtime logger -> artifact files | Untrusted/partial runtime state is serialized into benchmark contract artifacts |
| artifact files -> conformance validator | Validator trust depends on complete, canonical event stream presence |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01-01 | T | `TrajLogger` canonical JSONL writer | mitigate | Emit header via `CanonicalTrajectoryHeader` model and idempotent guard before step/score/metrics writes |
| T-07-01-02 | D | canonical event parser/consumer | mitigate | Add regression assertions that enforce exactly one header event and canonical schema fields |
</threat_model>

<verification>
Run:
- `UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_canonical_trajectory_contract.py`
</verification>

<success_criteria>
- `tests/protocol/test_canonical_trajectory_contract.py` asserts exactly one `header` event exists in runtime canonical JSONL.
- Runtime logger code path emits canonical header without breaking step/score event output.
</success_criteria>

<output>
After completion, create `.planning/phases/07-conformance-artifact-contract-closure/07-01-SUMMARY.md`
</output>
