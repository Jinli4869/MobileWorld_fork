---
phase: 02-tool-router-capability-policy
plan: 03
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/mobile_world/runtime/client.py
  - src/mobile_world/runtime/mcp_server.py
  - src/mobile_world/runtime/protocol/capability_policy.py
  - src/mobile_world/runtime/utils/trajectory_logger.py
autonomous: true
requirements: [TOOL-03]
user_setup: []
must_haves:
  truths:
    - "MCP registration/filtering supports config-driven allowlist and timeout."
    - "Tool manifest is persisted in artifacts."
  artifacts:
    - "src/mobile_world/runtime/client.py"
    - "src/mobile_world/runtime/utils/trajectory_logger.py"
  key_links:
    - "policy decision drives MCP reset_tools allowlist and timeout behavior."
---

<objective>
Refactor MCP registration to config-driven allowlist model and persist manifest.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/mobile_world/runtime/client.py
@src/mobile_world/runtime/mcp_server.py
@src/mobile_world/runtime/utils/trajectory_logger.py
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add MCP allowlist + timeout controls to runtime client path</name>
  <files>src/mobile_world/runtime/client.py, src/mobile_world/runtime/mcp_server.py</files>
  <action>Extend MCP env client reset logic to accept allowlist and timeout from policy decision/config.</action>
  <verify>rg -n "allowlist|timeout|set_mcp_timeout" src/mobile_world/runtime/client.py src/mobile_world/runtime/mcp_server.py</verify>
  <done>MCP runtime behavior can be policy-driven.</done>
</task>
<task type="auto">
  <name>Task 2: Persist deterministic tool manifest in trajectory artifacts</name>
  <files>src/mobile_world/runtime/utils/trajectory_logger.py</files>
  <action>Add log method for tool manifest and persist into both canonical meta and legacy log envelope.</action>
  <verify>rg -n "tool_manifest|log_tool_manifest" src/mobile_world/runtime/utils/trajectory_logger.py</verify>
  <done>Manifest is captured in run artifacts.</done>
</task>
</tasks>

<verification>
- [ ] `python3 -m compileall src/mobile_world/runtime`
- [ ] tool manifest fields appear in logger output path
</verification>

<output>
After completion, create `.planning/phases/02-tool-router-capability-policy/02-03-SUMMARY.md`
</output>
