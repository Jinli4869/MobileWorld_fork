---
phase: 08-framework-profile-policy-flow-repair
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mobile_world/core/runner.py
autonomous: true
requirements: [TOOL-02]
user_setup: []
must_haves:
  truths:
    - "Adapter-mode capability policy resolution uses framework profile identity when framework_profile is provided."
    - "Built-in mode capability policy resolution still uses agent_type when framework_profile is absent."
    - "Capability manifest persistence remains unchanged and deterministic."
  artifacts:
    - path: src/mobile_world/core/runner.py
      provides: "Effective policy profile identity selection wired into resolve_capability_policy input."
  key_links:
    - from: src/mobile_world/core/runner.py
      to: src/mobile_world/runtime/protocol/capability_policy.py
      via: resolve_capability_policy profile_name input
      pattern: "profile_name=.*effective_policy_profile"
---

<objective>
Bind capability-policy resolution to framework-profile identity in adapter mode while preserving built-in fallback semantics.

Purpose: Close the root cause behind profile-scoped policy mismatches in framework-profile eval runs.
Output: Narrow runner-path fix that computes one effective policy profile and passes it into capability resolution.
</objective>

<execution_context>
@/home/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/home/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-framework-profile-policy-flow-repair/08-RESEARCH.md
@.planning/phases/08-framework-profile-policy-flow-repair/08-PATTERNS.md
@.planning/phases/08-framework-profile-policy-flow-repair/08-VALIDATION.md
@src/mobile_world/core/runner.py
@src/mobile_world/runtime/protocol/capability_policy.py
@src/mobile_world/core/subcommands/eval.py

<interfaces>
From `src/mobile_world/core/runner.py`:
- `def _process_task_on_env(...) -> dict`

From `src/mobile_world/runtime/protocol/capability_policy.py`:
- `def resolve_capability_policy(..., profile_name: str, ...) -> CapabilityDecision`
- `CapabilityDecision.as_manifest() -> dict`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Use effective profile identity for capability resolution in runner</name>
  <files>src/mobile_world/core/runner.py</files>
  <read_first>
    - src/mobile_world/core/runner.py
    - src/mobile_world/runtime/protocol/capability_policy.py
    - src/mobile_world/core/subcommands/eval.py
  </read_first>
  <action>Inside `_process_task_on_env`, add `effective_policy_profile = framework_profile or agent_type` immediately before `resolve_capability_policy(...)`. Replace `profile_name=agent_type` with `profile_name=effective_policy_profile`. Keep all other resolver inputs and manifest logging calls unchanged (`traj_logger.log_tool_manifest(capability_manifest)` and `traj_logger.log_policy_manifest(capability_manifest)`).</action>
  <acceptance_criteria>
    - `rg -n "effective_policy_profile\s*=\s*framework_profile\s+or\s+agent_type" src/mobile_world/core/runner.py` returns one match.
    - `rg -n "profile_name\s*=\s*effective_policy_profile" src/mobile_world/core/runner.py` returns one match in the capability resolver call.
    - `rg -n "log_tool_manifest\(capability_manifest\)|log_policy_manifest\(capability_manifest\)" src/mobile_world/core/runner.py` still returns both calls.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "effective_policy_profile\s*=\s*framework_profile\s+or\s+agent_type|profile_name\s*=\s*effective_policy_profile|log_tool_manifest\(capability_manifest\)|log_policy_manifest\(capability_manifest\)" src/mobile_world/core/runner.py'</automated>
  </verify>
  <done>Runner capability-policy resolution is profile-bound in adapter mode and fallback-safe in built-in mode.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/framework config -> runner policy resolver | User-selected profile identity must be translated deterministically into policy matching input |
| runner policy decision -> manifest artifacts | Wrong profile identity would persist misleading policy manifests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01-01 | T | `_process_task_on_env` policy profile input | mitigate | Use explicit `effective_policy_profile = framework_profile or agent_type` and pass it directly to resolver |
| T-08-01-02 | R | capability manifest semantics | mitigate | Preserve single-source manifest logging calls unchanged while fixing only profile identity input |
</threat_model>

<verification>
Run:
- `UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_phase5_framework_profiles.py`
</verification>

<success_criteria>
- Runner computes and uses one explicit `effective_policy_profile` for policy resolution.
- Resolver input is `framework_profile` in adapter mode and `agent_type` otherwise.
- Existing manifest logging sequence remains intact.
</success_criteria>

<output>
After completion, create `.planning/phases/08-framework-profile-policy-flow-repair/08-01-SUMMARY.md`
</output>
