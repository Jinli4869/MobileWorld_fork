---
phase: 08-framework-profile-policy-flow-repair
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - tests/protocol/test_phase8_framework_profile_policy_flow.py
autonomous: true
requirements: [TOOL-02]
user_setup: []
must_haves:
  truths:
    - "Runner passes framework_profile to capability resolver when adapter-mode is selected."
    - "Policy manifest profile_name in artifacts matches the effective policy profile used by resolver."
    - "Built-in fallback path remains deterministic (profile_name == agent_type when framework_profile is absent)."
  artifacts:
    - path: tests/protocol/test_phase8_framework_profile_policy_flow.py
      provides: "Deterministic regression coverage for policy profile identity and artifact manifest alignment."
  key_links:
    - from: tests/protocol/test_phase8_framework_profile_policy_flow.py
      to: src/mobile_world/core/runner.py
      via: _process_task_on_env invocation with monkeypatched seams
      pattern: "_process_task_on_env"
    - from: tests/protocol/test_phase8_framework_profile_policy_flow.py
      to: src/mobile_world/runtime/protocol/capability_policy.py
      via: resolve_capability_policy capture and manifest assertions
      pattern: "resolve_capability_policy"
---

<objective>
Lock profile-bound policy semantics with focused runner-path regressions and manifest-alignment assertions.

Purpose: Prevent regressions where policy matching or persisted manifests drift away from framework-profile intent.
Output: New phase-8 protocol test module with deterministic fakes and exact-value assertions.
</objective>

<execution_context>
@/home/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/home/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-framework-profile-policy-flow-repair/08-RESEARCH.md
@.planning/phases/08-framework-profile-policy-flow-repair/08-PATTERNS.md
@.planning/phases/08-framework-profile-policy-flow-repair/08-VALIDATION.md
@src/mobile_world/core/runner.py
@src/mobile_world/runtime/protocol/capability_policy.py
@src/mobile_world/runtime/utils/trajectory_logger.py
@tests/protocol/test_phase4_nanobot_reference_integration.py
@tests/protocol/test_phase5_framework_profiles.py

<interfaces>
From `src/mobile_world/core/runner.py`:
- `_process_task_on_env(...)`
- `TrajLogger` artifact write path (`log_tool_manifest`, `log_policy_manifest`)

From `src/mobile_world/runtime/protocol/capability_policy.py`:
- `CapabilityDecision`
- `resolve_capability_policy(...)`
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create phase-8 protocol test module with runner test doubles</name>
  <files>tests/protocol/test_phase8_framework_profile_policy_flow.py</files>
  <read_first>
    - src/mobile_world/core/runner.py
    - src/mobile_world/runtime/protocol/capability_policy.py
    - tests/protocol/test_phase4_nanobot_reference_integration.py
    - tests/protocol/test_phase5_framework_profiles.py
  </read_first>
  <action>Create `tests/protocol/test_phase8_framework_profile_policy_flow.py` with lightweight fakes for env/agent/adapter and monkeypatch seams for `_execute_single_task`, `create_agent`, `create_framework_adapter`, and `create_evaluator` so `_process_task_on_env` can run deterministically without emulator dependencies.</action>
  <acceptance_criteria>
    - `rg -n "class DummyEnv|def _run_process_task\(" tests/protocol/test_phase8_framework_profile_policy_flow.py` returns matches.
    - Test module does not require Android backend or Docker to execute.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "class DummyEnv|def _run_process_task\(" tests/protocol/test_phase8_framework_profile_policy_flow.py'</automated>
  </verify>
  <done>Reusable deterministic test harness exists for phase-8 runner-path assertions.</done>
</task>

<task type="auto">
  <name>Task 2: Assert runner uses framework profile for policy resolution</name>
  <files>tests/protocol/test_phase8_framework_profile_policy_flow.py</files>
  <read_first>
    - tests/protocol/test_phase8_framework_profile_policy_flow.py
    - src/mobile_world/core/runner.py
    - src/mobile_world/runtime/protocol/capability_policy.py
  </read_first>
  <action>Add `test_runner_uses_framework_profile_for_policy_resolution` that monkeypatches `resolve_capability_policy` to capture `profile_name`, invokes `_process_task_on_env(..., framework_profile="nanobot_opengui", agent_type="qwen3vl", enable_mcp=False)`, and asserts captured `profile_name == "nanobot_opengui"`.</action>
  <acceptance_criteria>
    - `rg -n "test_runner_uses_framework_profile_for_policy_resolution|nanobot_opengui" tests/protocol/test_phase8_framework_profile_policy_flow.py` returns matches.
    - Test asserts exact equality for captured resolver `profile_name`.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_phase8_framework_profile_policy_flow.py::test_runner_uses_framework_profile_for_policy_resolution</automated>
  </verify>
  <done>Adapter-mode policy-resolution identity mismatch is regression-protected.</done>
</task>

<task type="auto">
  <name>Task 3: Assert policy manifest profile_name matches effective profile in artifacts</name>
  <files>tests/protocol/test_phase8_framework_profile_policy_flow.py</files>
  <read_first>
    - tests/protocol/test_phase8_framework_profile_policy_flow.py
    - src/mobile_world/runtime/utils/trajectory_logger.py
    - src/mobile_world/runtime/protocol/capability_policy.py
  </read_first>
  <action>Add `test_policy_manifest_profile_name_matches_effective_profile` that runs `_process_task_on_env` twice (framework mode and built-in fallback mode), then reads generated `traj.json` and `traj.meta.json` to assert `policy_manifest.profile_name` equals `framework_profile` in adapter mode and `agent_type` when `framework_profile` is absent.</action>
  <acceptance_criteria>
    - `rg -n "test_policy_manifest_profile_name_matches_effective_profile|policy_manifest|profile_name" tests/protocol/test_phase8_framework_profile_policy_flow.py` returns matches.
    - Assertions cover both legacy and canonical metadata artifacts.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_phase8_framework_profile_policy_flow.py::test_policy_manifest_profile_name_matches_effective_profile</automated>
  </verify>
  <done>Artifact-level policy profile alignment is enforced for both adapter and built-in branches.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| runner policy identity -> serialized manifests | Profile mismatch could silently produce incorrect artifact semantics |
| test harness -> runner seam behavior | Incomplete seam mocking can hide real path behavior |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-02-01 | T | policy resolver input branch | mitigate | Explicit regression asserting resolver `profile_name` equals selected `framework_profile` |
| T-08-02-02 | R | policy manifest artifact semantics | mitigate | Assert `policy_manifest.profile_name` exact values in both legacy and canonical artifacts |
| T-08-02-03 | D | test realism drift | mitigate | Run `_process_task_on_env` with patched seams instead of testing detached helper logic |
</threat_model>

<verification>
Run:
- `UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_phase8_framework_profile_policy_flow.py`
</verification>

<success_criteria>
- New phase-8 regression module exists and runs locally without emulator backends.
- Resolver input profile identity is asserted for framework mode.
- Persisted policy manifest profile identity is asserted for framework and fallback modes.
</success_criteria>

<output>
After completion, create `.planning/phases/08-framework-profile-policy-flow-repair/08-02-SUMMARY.md`
</output>
