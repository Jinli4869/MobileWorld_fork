---
phase: 08-framework-profile-policy-flow-repair
plan: 03
type: execute
wave: 3
depends_on: [08-01, 08-02]
files_modified:
  - tests/protocol/test_phase8_framework_profile_policy_flow.py
  - tests/protocol/test_phase5_framework_profiles.py
autonomous: true
requirements: [INTG-03, TOOL-02]
user_setup: []
must_haves:
  truths:
    - "Eval CLI framework-profile inputs are forwarded into runner invocation without profile-name drift."
    - "Framework-profile eval flow preserves profile semantics from CLI/config parsing to runner capability-policy resolution."
    - "Phase-8 regression suite provides one command to validate both runner semantics and eval flow semantics."
  artifacts:
    - path: tests/protocol/test_phase8_framework_profile_policy_flow.py
      provides: "Flow-level regression for eval -> runner framework-profile propagation semantics."
    - path: tests/protocol/test_phase5_framework_profiles.py
      provides: "CLI-profile contract assertions remain aligned with framework-profile flow behavior."
  key_links:
    - from: tests/protocol/test_phase8_framework_profile_policy_flow.py
      to: src/mobile_world/core/subcommands/eval.py
      via: execute() invocation with parser args and monkeypatched run_agent_with_evaluation
      pattern: "framework_profile"
    - from: tests/protocol/test_phase8_framework_profile_policy_flow.py
      to: src/mobile_world/core/runner.py
      via: captured kwargs include framework_profile and built-in agent_type
      pattern: "run_agent_with_evaluation"
---

<objective>
Close INTG-03 by regression-testing framework-profile eval flow semantics through CLI-to-runner handoff.

Purpose: Ensure selected framework profile in eval entrypoints is preserved through execution path and cannot regress back to agent-type keyed behavior.
Output: Flow regression tests and consolidated verification commands for the entire phase-8 scope.
</objective>

<execution_context>
@/home/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/home/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/08-framework-profile-policy-flow-repair/08-RESEARCH.md
@.planning/phases/08-framework-profile-policy-flow-repair/08-PATTERNS.md
@.planning/phases/08-framework-profile-policy-flow-repair/08-VALIDATION.md
@src/mobile_world/core/subcommands/eval.py
@src/mobile_world/core/cli.py
@src/mobile_world/core/runner.py
@tests/protocol/test_phase8_framework_profile_policy_flow.py
@tests/protocol/test_phase5_framework_profiles.py

<interfaces>
From `src/mobile_world/core/subcommands/eval.py`:
- `async def execute(args: argparse.Namespace) -> None`
- framework profile config loading + forwarding to `run_agent_with_evaluation(...)`

From `src/mobile_world/core/cli.py`:
- parser wiring for eval command flags
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add eval flow regression for framework-profile propagation semantics</name>
  <files>tests/protocol/test_phase8_framework_profile_policy_flow.py</files>
  <read_first>
    - tests/protocol/test_phase8_framework_profile_policy_flow.py
    - src/mobile_world/core/subcommands/eval.py
    - src/mobile_world/core/cli.py
  </read_first>
  <action>Add `test_eval_framework_profile_flow_preserves_profile_semantics` that builds eval args via parser, monkeypatches `run_agent_with_evaluation` inside eval subcommand to capture kwargs, executes `eval.execute(args)` (via `asyncio.run`), and asserts forwarded kwargs contain exact `framework_profile` and `agent_type` values expected by runner invocation.</action>
  <acceptance_criteria>
    - `rg -n "test_eval_framework_profile_flow_preserves_profile_semantics|run_agent_with_evaluation|asyncio\.run" tests/protocol/test_phase8_framework_profile_policy_flow.py` returns matches.
    - Test asserts exact forwarded `framework_profile` value in captured kwargs.
    - Test asserts `agent_type` remains unchanged while framework profile is forwarded.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_phase8_framework_profile_policy_flow.py::test_eval_framework_profile_flow_preserves_profile_semantics</automated>
  </verify>
  <done>CLI-to-runner framework-profile propagation is regression-protected.</done>
</task>

<task type="auto">
  <name>Task 2: Extend framework profile contract tests with phase-8 semantic guard</name>
  <files>tests/protocol/test_phase5_framework_profiles.py</files>
  <read_first>
    - tests/protocol/test_phase5_framework_profiles.py
    - src/mobile_world/core/subcommands/eval.py
    - .planning/phases/08-framework-profile-policy-flow-repair/08-RESEARCH.md
  </read_first>
  <action>Add or update one test in `test_phase5_framework_profiles.py` to assert framework-config-driven profile selection remains deterministic (e.g., framework config payload selecting `framework_profile` is consumed exactly and not overwritten by unrelated defaults). Keep assertions concrete and value-based.</action>
  <acceptance_criteria>
    - `rg -n "framework_profile" tests/protocol/test_phase5_framework_profiles.py` includes explicit semantic assertions beyond parser flag presence.
    - Test failure messages clearly indicate profile-semantics regression.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_phase5_framework_profiles.py</automated>
  </verify>
  <done>Existing framework-profile contract tests include phase-8 semantic guardrails.</done>
</task>

<task type="auto">
  <name>Task 3: Add consolidated phase-8 regression command coverage</name>
  <files>tests/protocol/test_phase8_framework_profile_policy_flow.py, .planning/phases/08-framework-profile-policy-flow-repair/08-VALIDATION.md</files>
  <read_first>
    - tests/protocol/test_phase8_framework_profile_policy_flow.py
    - tests/protocol/test_phase5_framework_profiles.py
    - .planning/phases/08-framework-profile-policy-flow-repair/08-VALIDATION.md
  </read_first>
  <action>In `tests/protocol/test_phase8_framework_profile_policy_flow.py`, add a module-level constant named `PHASE8_COMBINED_REGRESSION_CMD` with exact value `UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_phase8_framework_profile_policy_flow.py tests/protocol/test_phase5_framework_profiles.py`, and add a short comment referencing this as the canonical phase-8 verification command. In `.planning/phases/08-framework-profile-policy-flow-repair/08-VALIDATION.md`, update the phase-wave sampling bullet to use this same exact command string.</action>
  <acceptance_criteria>
    - `08-VALIDATION.md` and test module command guidance both reference `UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_phase8_framework_profile_policy_flow.py tests/protocol/test_phase5_framework_profiles.py`.
    - Combined command exits successfully when phase-8 implementation is complete.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_phase8_framework_profile_policy_flow.py tests/protocol/test_phase5_framework_profiles.py</automated>
  </verify>
  <done>Phase-8 flow verification is one-command reproducible for execution and review workflows.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| eval CLI args/config -> runner kwargs | Framework-profile identity can be lost or overwritten if forwarding semantics drift |
| runner kwargs -> policy resolution path | Mis-forwarded identity reintroduces TOOL-02/INTG-03 gaps |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03-01 | T | eval-to-runner profile forwarding | mitigate | Add regression that captures `run_agent_with_evaluation` kwargs and asserts exact forwarded profile semantics |
| T-08-03-02 | R | framework profile config semantics | mitigate | Extend phase-5 profile contract tests with explicit deterministic profile selection assertions |
| T-08-03-03 | D | fragmented verification commands | mitigate | Publish one consolidated phase-8 regression command and enforce it in validation map |
</threat_model>

<verification>
Run:
- `UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_phase8_framework_profile_policy_flow.py tests/protocol/test_phase5_framework_profiles.py`
- `UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol`
</verification>

<success_criteria>
- Eval framework-profile flow propagation is covered by deterministic automated tests.
- Phase-5 framework profile tests contain semantic (not just parser) assertions relevant to phase-8 guarantees.
- One combined phase-8 regression command is documented and executable.
</success_criteria>

<output>
After completion, create `.planning/phases/08-framework-profile-policy-flow-repair/08-03-SUMMARY.md`
</output>
