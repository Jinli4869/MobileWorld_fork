---
phase: 03-evaluator-unification
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/mobile_world/runtime/protocol/evaluator.py
  - src/mobile_world/core/runner.py
  - src/mobile_world/runtime/utils/trajectory_logger.py
autonomous: true
requirements: [EVAL-02]
user_setup: []
must_haves:
  truths:
    - "Existing deterministic task scoring remains primary signal via evaluator interface."
  artifacts:
    - "src/mobile_world/core/runner.py"
  key_links:
    - "runner end-of-task scoring goes through task-native evaluator."
---

<objective>
Bridge existing task-native scoring into evaluator interface.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Implement task-native evaluator bridge</name>
  <files>src/mobile_world/runtime/protocol/evaluator.py</files>
  <action>Implement evaluator that wraps `env.get_task_score` and emits standardized score/reason/evidence output.</action>
  <verify>rg -n "class TaskNativeEvaluator" src/mobile_world/runtime/protocol/evaluator.py</verify>
  <done>Task-native scoring wrapped by evaluator contract.</done>
</task>
<task type="auto">
  <name>Task 2: Integrate runner scoring path with evaluator abstraction</name>
  <files>src/mobile_world/core/runner.py, src/mobile_world/runtime/utils/trajectory_logger.py</files>
  <action>Replace direct `env.get_task_score` call with evaluator execution and audit persistence.</action>
  <verify>rg -n "create_evaluator|evaluate\\(|log_evaluator_audit" src/mobile_world/core/runner.py src/mobile_world/runtime/utils/trajectory_logger.py</verify>
  <done>Runner scoring is evaluator-driven while deterministic behavior preserved.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/03-evaluator-unification/03-02-SUMMARY.md`
</output>
