---
phase: 03-evaluator-unification
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - src/mobile_world/runtime/protocol/evaluator.py
  - src/mobile_world/core/subcommands/eval.py
  - src/mobile_world/core/runner.py
autonomous: true
requirements: [EVAL-03]
user_setup: []
must_haves:
  truths:
    - "Optional trajectory judge can be enabled without changing final artifact schema."
    - "Judge model/config is explicitly recorded in evaluator audit."
  artifacts:
    - "src/mobile_world/runtime/protocol/evaluator.py"
  key_links:
    - "eval CLI flags feed judge config into evaluator creation."
---

<objective>
Integrate optional trajectory judge backend with explicit config recording.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<tasks>
<task type="auto">
  <name>Task 1: Add optional trajectory judge evaluator path</name>
  <files>src/mobile_world/runtime/protocol/evaluator.py</files>
  <action>Add optional judge mode that records config and judge verdict while preserving deterministic score as primary output.</action>
  <verify>rg -n "TrajectoryJudge|enable_trajectory_judge|judge_model" src/mobile_world/runtime/protocol/evaluator.py</verify>
  <done>Optional judge path integrated into evaluator pipeline.</done>
</task>
<task type="auto">
  <name>Task 2: Expose judge config flags in eval flow</name>
  <files>src/mobile_world/core/subcommands/eval.py, src/mobile_world/core/runner.py</files>
  <action>Add CLI options and runner plumbing for optional judge enable/config recording.</action>
  <verify>rg -n "enable_trajectory_judge|judge_model|judge_api" src/mobile_world/core/subcommands/eval.py src/mobile_world/core/runner.py</verify>
  <done>Judge integration is configurable and auditable.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/03-evaluator-unification/03-03-SUMMARY.md`
</output>
