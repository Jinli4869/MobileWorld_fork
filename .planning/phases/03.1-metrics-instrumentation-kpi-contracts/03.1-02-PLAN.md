---
phase: 03.1-metrics-instrumentation-kpi-contracts
plan: 02
type: execute
wave: 2
depends_on: ["03.1-01"]
files_modified:
  - src/mobile_world/runtime/protocol/metrics.py
  - src/mobile_world/core/runner.py
autonomous: true
requirements: [METR-01, METR-02]
user_setup: []
must_haves:
  truths:
    - "Per-step token deltas are captured and queryable."
    - "TTFT supports explicit fallback quality semantics."
  artifacts:
    - "src/mobile_world/runtime/protocol/metrics.py"
  key_links:
    - "runner computes metrics with fallback flags (native/estimated/unavailable)."
---

<objective>
Add token and TTFT collection adapters with fallback policy.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Build metrics collector utility</name>
  <files>src/mobile_world/runtime/protocol/metrics.py</files>
  <action>Implement token delta extraction, TTFT/TTFA/TTS calculation, percentile helpers, and quality-flaged summaries.</action>
  <verify>rg -n "MetricsCollector|ttft|quality_flags|avg_total_tokens_per_step" src/mobile_world/runtime/protocol/metrics.py</verify>
  <done>Metrics collector supports token and TTFT fallback policies.</done>
</task>
<task type="auto">
  <name>Task 2: Wire collector into runner step loop</name>
  <files>src/mobile_world/core/runner.py</files>
  <action>Collect per-step usage/latency in runner and persist summary via traj logger hooks.</action>
  <verify>rg -n "MetricsCollector|log_step_metrics|log_metrics_summary" src/mobile_world/core/runner.py</verify>
  <done>Runner emits per-step and run-level metrics artifacts.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/03.1-metrics-instrumentation-kpi-contracts/03.1-02-SUMMARY.md`
</output>
