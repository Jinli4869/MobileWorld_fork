---
phase: 03.1-metrics-instrumentation-kpi-contracts
plan: 03
type: execute
wave: 3
depends_on: ["03.1-02"]
files_modified:
  - src/mobile_world/core/runner.py
  - src/mobile_world/core/subcommands/eval.py
autonomous: true
requirements: [METR-03, METR-05]
user_setup: []
must_haves:
  truths:
    - "Latency and reliability KPIs are aggregated at run level."
  artifacts:
    - "Eval report includes run-level KPI summary fields."
  key_links:
    - "task results expose per-task metrics summaries."
---

<objective>
Add latency/reliability aggregators and per-run KPI summaries.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Persist per-task KPI summary into runner results</name>
  <files>src/mobile_world/core/runner.py</files>
  <action>Include `metrics` in each finished task record by loading per-task `metrics.json` artifact.</action>
  <verify>rg -n "metrics_path|METRICS_FILE_NAME|\"metrics\"" src/mobile_world/core/runner.py</verify>
  <done>Runner output carries per-task KPI summaries.</done>
</task>
<task type="auto">
  <name>Task 2: Add run-level KPI rollups to eval report</name>
  <files>src/mobile_world/core/subcommands/eval.py</files>
  <action>Compute run-level averages for token-per-step, TTFT, and tool success rate in report summary.</action>
  <verify>rg -n "avg_tokens_per_step_run_mean|avg_ttft_ms_run_mean|avg_tool_success_rate_run_mean" src/mobile_world/core/subcommands/eval.py</verify>
  <done>Eval report exposes run-level KPI aggregates.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/03.1-metrics-instrumentation-kpi-contracts/03.1-03-SUMMARY.md`
</output>
