---
phase: 09-reproducibility-agreement-gate-hardening
plan: 03
type: execute
wave: 3
depends_on: [09-01, 09-02]
files_modified:
  - tests/protocol/test_phase9_reproducibility_agreement_gate.py
  - tests/protocol/test_phase6_reporting_conformance_reproducibility.py
autonomous: true
requirements: [COMP-03, METR-06, METR-07]
user_setup: []
must_haves:
  truths:
    - "Regression tests verify reproducibility passes when variance passes and judge agreement is unavailable."
    - "Regression tests verify agreement threshold failure still fails reproducibility when judge checks are available."
    - "Regression tests assert explicit agreement availability/outcome fields to prevent semantic drift."
  artifacts:
    - path: tests/protocol/test_phase9_reproducibility_agreement_gate.py
      provides: "Phase-9 focused agreement-gate regression coverage."
    - path: tests/protocol/test_phase6_reporting_conformance_reproducibility.py
      provides: "Legacy phase-6 reproducibility tests aligned with hardened semantics."
  key_links:
    - from: tests/protocol/test_phase9_reproducibility_agreement_gate.py
      to: src/mobile_world/runtime/protocol/reproducibility.py
      via: direct evaluate_reproducibility assertions
      pattern: evaluate_reproducibility
    - from: tests/protocol/test_phase6_reporting_conformance_reproducibility.py
      to: src/mobile_world/core/subcommands/benchmark.py
      via: benchmark parser/behavior compatibility expectations
      pattern: benchmark
---

<objective>
Add deterministic regression coverage for judge-available and judge-unavailable reproducibility scenarios, and align existing protocol tests with hardened report semantics.

Purpose: Lock Phase 9 behavior so future changes cannot reintroduce false hard-fail or weaken agreement enforcement.
Output: New phase-9 protocol tests plus updated phase-6 assertions that verify the new field contract and gate behavior.
</objective>

<execution_context>
@/home/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/home/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-reproducibility-agreement-gate-hardening/09-RESEARCH.md
@.planning/phases/09-reproducibility-agreement-gate-hardening/09-PATTERNS.md
@.planning/phases/09-reproducibility-agreement-gate-hardening/09-VALIDATION.md
@src/mobile_world/runtime/protocol/reproducibility.py
@tests/protocol/test_phase6_reporting_conformance_reproducibility.py

<interfaces>
From `src/mobile_world/runtime/protocol/reproducibility.py`:
- `evaluate_reproducibility(...)`
- `evaluation_quality` and gate-summary report fields
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create phase-9 focused reproducibility agreement-gate test module</name>
  <files>tests/protocol/test_phase9_reproducibility_agreement_gate.py</files>
  <read_first>
    - tests/protocol/test_phase6_reporting_conformance_reproducibility.py
    - src/mobile_world/runtime/protocol/reproducibility.py
    - .planning/phases/09-reproducibility-agreement-gate-hardening/09-RESEARCH.md
  </read_first>
  <action>Create `tests/protocol/test_phase9_reproducibility_agreement_gate.py` with synthetic run-root fixtures and at least three deterministic tests: (1) stability passes with agreement unavailable and overall `ok` remains true, (2) judge-available disagreement below threshold fails overall `ok`, and (3) report fields (`agreement_available`, `agreement_status`, `agreement_passed`, gate summary) have exact expected values for unavailable and enforced branches.</action>
  <acceptance_criteria>
    - `rg -n "test_reproducibility_stability_gate_passes_without_judge_data|test_reproducibility_fails_when_judge_available_and_below_threshold|agreement_available|agreement_status" tests/protocol/test_phase9_reproducibility_agreement_gate.py` returns matches.
    - Tests use local synthetic artifacts only and do not require external judge backends.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_phase9_reproducibility_agreement_gate.py</automated>
  </verify>
  <done>Phase-9 branch semantics are regression-protected with deterministic local tests.</done>
</task>

<task type="auto">
  <name>Task 2: Align phase-6 reproducibility assertions with hardened field contract</name>
  <files>tests/protocol/test_phase6_reporting_conformance_reproducibility.py</files>
  <read_first>
    - tests/protocol/test_phase6_reporting_conformance_reproducibility.py
    - src/mobile_world/runtime/protocol/reproducibility.py
    - tests/protocol/test_phase9_reproducibility_agreement_gate.py
  </read_first>
  <action>Update existing reproducibility assertions in phase-6 tests to validate new agreement-availability fields while preserving existing variance and agreement-rate checks. Ensure assertions include explicit expectations for agreement gate status and top-level `ok` behavior under available judge data.</action>
  <acceptance_criteria>
    - `rg -n "agreement_available|agreement_status|agreement_passed|gate_summary" tests/protocol/test_phase6_reporting_conformance_reproducibility.py` returns matches.
    - Existing phase-6 reproducibility regression still passes with hardened semantics.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_phase6_reporting_conformance_reproducibility.py::test_reproducibility_reports_variance_and_judge_agreement</automated>
  </verify>
  <done>Legacy reproducibility coverage remains compatible and now checks Phase-9 field semantics.</done>
</task>

<task type="auto">
  <name>Task 3: Add one canonical phase-9 regression command and run combined verification</name>
  <files>tests/protocol/test_phase9_reproducibility_agreement_gate.py, .planning/phases/09-reproducibility-agreement-gate-hardening/09-VALIDATION.md</files>
  <read_first>
    - tests/protocol/test_phase9_reproducibility_agreement_gate.py
    - tests/protocol/test_phase6_reporting_conformance_reproducibility.py
    - .planning/phases/09-reproducibility-agreement-gate-hardening/09-VALIDATION.md
  </read_first>
  <action>Define a module-level constant `PHASE9_COMBINED_REGRESSION_CMD` in `test_phase9_reproducibility_agreement_gate.py` with exact value `UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_phase9_reproducibility_agreement_gate.py tests/protocol/test_phase6_reporting_conformance_reproducibility.py`, and ensure `09-VALIDATION.md` references that same exact command in quick/full verification guidance.</action>
  <acceptance_criteria>
    - `rg -n "PHASE9_COMBINED_REGRESSION_CMD|test_phase9_reproducibility_agreement_gate.py tests/protocol/test_phase6_reporting_conformance_reproducibility.py" tests/protocol/test_phase9_reproducibility_agreement_gate.py .planning/phases/09-reproducibility-agreement-gate-hardening/09-VALIDATION.md` returns matches.
    - Combined regression command exits successfully after task completion.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_phase9_reproducibility_agreement_gate.py tests/protocol/test_phase6_reporting_conformance_reproducibility.py</automated>
  </verify>
  <done>Phase-9 execution/review workflows have one-command reproducible regression verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reproducibility protocol semantics -> regression suite | Missing branch coverage can allow gate regressions to reappear silently |
| report-field contract -> downstream consumers/tests | Field drift without tests can break automation and human interpretation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-03-01 | R | judge-unavailable branch behavior | mitigate | Add explicit pass-case regression for unavailable agreement + stable variance |
| T-09-03-02 | T | judge-available threshold enforcement | mitigate | Add explicit fail-case regression for below-threshold agreement with available checks |
| T-09-03-03 | D | verification command drift | mitigate | Define one canonical combined regression command and mirror it in validation artifact |
</threat_model>

<verification>
Run:
- `UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_phase9_reproducibility_agreement_gate.py`
- `UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_phase6_reporting_conformance_reproducibility.py::test_reproducibility_reports_variance_and_judge_agreement`
- `UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_phase9_reproducibility_agreement_gate.py tests/protocol/test_phase6_reporting_conformance_reproducibility.py`
</verification>

<success_criteria>
- Judge-unavailable and judge-available reproducibility branches are both covered by deterministic tests.
- Phase-6 reproducibility regression remains green with updated field semantics.
- Canonical combined phase-9 regression command exists and is documented consistently.
</success_criteria>

<output>
After completion, create `.planning/phases/09-reproducibility-agreement-gate-hardening/09-03-SUMMARY.md`
</output>
