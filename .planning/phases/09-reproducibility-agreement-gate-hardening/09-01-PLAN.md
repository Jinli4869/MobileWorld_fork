---
phase: 09-reproducibility-agreement-gate-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mobile_world/runtime/protocol/reproducibility.py
autonomous: true
requirements: [METR-06, METR-07]
user_setup: []
must_haves:
  truths:
    - "Stability variance pass/fail is evaluated independently from judge-agreement availability."
    - "Runs with no judge checks do not false-fail the top-level reproducibility gate when stability passes."
    - "Judge-agreement threshold remains enforced whenever judge checks are available."
  artifacts:
    - path: src/mobile_world/runtime/protocol/reproducibility.py
      provides: "Hardened reproducibility gate logic with explicit agreement-availability branch handling."
  key_links:
    - from: src/mobile_world/runtime/protocol/reproducibility.py
      to: src/mobile_world/core/subcommands/benchmark.py
      via: report['ok'] consumed for CLI PASS/FAIL status
      pattern: '"ok":'
---

<objective>
Refine reproducibility gate logic so missing judge-agreement signals do not force false hard-failure while preserving deterministic stability checks and enforced threshold behavior when judge data exists.

Purpose: Close the root semantic gap in reproducibility evaluation before expanding report/CLI fields.
Output: `evaluate_reproducibility(...)` computes overall gate outcome with explicit available-vs-unavailable judge branch semantics.
</objective>

<execution_context>
@/home/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/home/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-reproducibility-agreement-gate-hardening/09-RESEARCH.md
@.planning/phases/09-reproducibility-agreement-gate-hardening/09-PATTERNS.md
@.planning/phases/09-reproducibility-agreement-gate-hardening/09-VALIDATION.md
@src/mobile_world/runtime/protocol/reproducibility.py
@src/mobile_world/core/subcommands/benchmark.py
@tests/protocol/test_phase6_reporting_conformance_reproducibility.py

<interfaces>
From `src/mobile_world/runtime/protocol/reproducibility.py`:
- `evaluate_reproducibility(..., variance_threshold, judge_agreement_threshold) -> dict[str, Any]`
- `evaluation_quality` and top-level `ok` report fields
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decouple stability gate from judge-agreement availability in reproducibility core logic</name>
  <files>src/mobile_world/runtime/protocol/reproducibility.py</files>
  <read_first>
    - src/mobile_world/runtime/protocol/reproducibility.py
    - tests/protocol/test_phase6_reporting_conformance_reproducibility.py
    - src/mobile_world/core/subcommands/benchmark.py
  </read_first>
  <action>Inside `evaluate_reproducibility`, compute `agreement_available = judge_total > 0`, then compute agreement-threshold pass only when available. Update top-level `ok` so it is `variance_ok` AND agreement-threshold pass when agreement is available, otherwise `variance_ok` alone. Keep variance computation, per-task rows, and threshold inputs unchanged. Ensure the available branch still uses exact threshold comparison against `judge_agreement_threshold`.</action>
  <acceptance_criteria>
    - `rg -n "agreement_available\s*=\s*judge_total\s*>\s*0" src/mobile_world/runtime/protocol/reproducibility.py` returns a match.
    - `rg -n "judge_agreement_threshold" src/mobile_world/runtime/protocol/reproducibility.py` still shows threshold enforcement logic on the agreement-available branch.
    - `rg -n '"ok"\s*:\s*' src/mobile_world/runtime/protocol/reproducibility.py` shows top-level `ok` no longer hard-requires non-null agreement rate.
  </acceptance_criteria>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_phase6_reporting_conformance_reproducibility.py::test_reproducibility_reports_variance_and_judge_agreement</automated>
  </verify>
  <done>Reproducibility gate semantics are corrected for missing-judge scenarios without weakening agreement enforcement when judge data exists.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| evaluator audit inputs -> reproducibility gate outcome | Missing judge checks must not be interpreted as failed agreement by default |
| protocol gate outcome -> benchmark CLI/user decisions | Incorrect `ok` semantics can block valid deterministic runs or hide real agreement failures |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01-01 | T | `evaluate_reproducibility` judge branch | mitigate | Introduce explicit availability branch and keep threshold enforcement only when agreement data exists |
| T-09-01-02 | R | top-level reproducibility gate output | mitigate | Preserve deterministic variance gate and make overall `ok` composition explicit in code/tests |
</threat_model>

<verification>
Run:
- `UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q tests/protocol/test_phase6_reporting_conformance_reproducibility.py::test_reproducibility_reports_variance_and_judge_agreement`
</verification>

<success_criteria>
- Stability gate remains deterministic and unchanged in behavior.
- Missing judge data no longer hard-fails reproducibility by default.
- Agreement threshold is still enforced for agreement-available runs.
</success_criteria>

<output>
After completion, create `.planning/phases/09-reproducibility-agreement-gate-hardening/09-01-SUMMARY.md`
</output>
