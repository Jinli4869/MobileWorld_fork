---
phase: 09-reproducibility-agreement-gate-hardening
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/mobile_world/runtime/protocol/reproducibility.py
  - src/mobile_world/core/subcommands/benchmark.py
  - docs/benchmark_reporting.md
autonomous: true
requirements: [METR-06, METR-07]
user_setup: []
must_haves:
  truths:
    - "Reproducibility report explicitly distinguishes judge-agreement availability from threshold pass/fail outcome."
    - "CLI reproducibility output surfaces agreement gate state (passed/failed/unavailable) alongside overall status."
    - "Operator docs define the new agreement availability and threshold outcome fields."
  artifacts:
    - path: src/mobile_world/runtime/protocol/reproducibility.py
      provides: "Explicit report fields for agreement availability and threshold outcomes."
    - path: src/mobile_world/core/subcommands/benchmark.py
      provides: "Operator-facing reproducibility status summary that includes agreement state."
    - path: docs/benchmark_reporting.md
      provides: "Documented reproducibility output semantics for missing-judge runs."
  key_links:
    - from: src/mobile_world/runtime/protocol/reproducibility.py
      to: src/mobile_world/core/subcommands/benchmark.py
      via: evaluation_quality and gate-summary fields consumed for output messaging
      pattern: agreement_available
---

<objective>
Add explicit report and CLI semantics for agreement availability and threshold outcomes so reproducibility decisions are transparent and auditable.

Purpose: Satisfy Phase 9 requirement that stability and agreement states are distinguishable in artifacts and command output.
Output: Reproducibility report exposes availability-aware agreement fields; benchmark CLI and docs reflect the hardened contract.
</objective>

<execution_context>
@/home/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/home/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-reproducibility-agreement-gate-hardening/09-RESEARCH.md
@.planning/phases/09-reproducibility-agreement-gate-hardening/09-PATTERNS.md
@.planning/phases/09-reproducibility-agreement-gate-hardening/09-VALIDATION.md
@src/mobile_world/runtime/protocol/reproducibility.py
@src/mobile_world/core/subcommands/benchmark.py
@docs/benchmark_reporting.md

<interfaces>
From `src/mobile_world/runtime/protocol/reproducibility.py`:
- report payload keys under `stability_metrics`, `evaluation_quality`, and top-level `ok`

From `src/mobile_world/core/subcommands/benchmark.py`:
- reproducibility branch in `execute(args)` that prints status summary
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add explicit agreement-availability and gate-outcome fields to reproducibility report</name>
  <files>src/mobile_world/runtime/protocol/reproducibility.py</files>
  <read_first>
    - src/mobile_world/runtime/protocol/reproducibility.py
    - .planning/phases/09-reproducibility-agreement-gate-hardening/09-RESEARCH.md
    - docs/benchmark_reporting.md
  </read_first>
  <action>Extend the returned report shape so `evaluation_quality` includes `agreement_available`, `agreement_passed`, and a deterministic status field (for example `agreement_status` with `passed`, `failed`, or `unavailable`). Add a top-level gate summary field that explicitly reports stability gate result, agreement gate result (or unavailable), and final overall status. Keep existing keys (`judge_agreement_rate`, `agreement_threshold`, `judge_checks_total`, and `ok`) present for compatibility.</action>
  <acceptance_criteria>
    - `rg -n "agreement_available|agreement_status|agreement_passed" src/mobile_world/runtime/protocol/reproducibility.py` returns matches.
    - `rg -n "gate_summary|stability_metrics" src/mobile_world/runtime/protocol/reproducibility.py` shows explicit stability-vs-agreement outcome fields.
    - Existing keys `judge_agreement_rate`, `agreement_threshold`, `judge_checks_total`, and `ok` still exist in the returned payload.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "agreement_available|agreement_status|agreement_passed|gate_summary|judge_agreement_rate|agreement_threshold|judge_checks_total|\"ok\"" src/mobile_world/runtime/protocol/reproducibility.py'</automated>
  </verify>
  <done>Reproducibility payload now distinguishes unavailable agreement from failed agreement while preserving compatibility keys.</done>
</task>

<task type="auto">
  <name>Task 2: Surface agreement availability/outcome in benchmark reproducibility CLI output</name>
  <files>src/mobile_world/core/subcommands/benchmark.py</files>
  <read_first>
    - src/mobile_world/core/subcommands/benchmark.py
    - src/mobile_world/runtime/protocol/reproducibility.py
    - docs/benchmark_reporting.md
  </read_first>
  <action>In the `benchmark reproducibility` command branch, keep existing PASS/FAIL summary but append explicit agreement state derived from report fields (`passed`, `failed`, or `unavailable`) and include judge-check count context so operators can differentiate missing-data runs from failing-agreement runs without opening JSON output.</action>
  <acceptance_criteria>
    - `rg -n "Reproducibility status|agreement" src/mobile_world/core/subcommands/benchmark.py` shows output text including agreement state.
    - CLI summary still includes common task count and PASS/FAIL status.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "Reproducibility status|agreement" src/mobile_world/core/subcommands/benchmark.py'</automated>
  </verify>
  <done>CLI summaries expose agreement availability/outcome in a single-line operator-readable status message.</done>
</task>

<task type="auto">
  <name>Task 3: Document hardened reproducibility semantics and new fields</name>
  <files>docs/benchmark_reporting.md</files>
  <read_first>
    - docs/benchmark_reporting.md
    - src/mobile_world/runtime/protocol/reproducibility.py
    - src/mobile_world/core/subcommands/benchmark.py
  </read_first>
  <action>Update the reproducibility section to document that agreement can be unavailable, describe how overall PASS is determined in that branch, and list the new output fields (`agreement_available`, `agreement_status`, `agreement_passed`, and gate summary fields) with concise operator interpretation guidance.</action>
  <acceptance_criteria>
    - `rg -n "agreement_available|agreement_status|unavailable|gate" docs/benchmark_reporting.md` returns matches in reproducibility section.
    - Documentation explicitly states that agreement threshold enforcement still applies when judge checks exist.
  </acceptance_criteria>
  <verify>
    <automated>bash -lc 'rg -n "agreement_available|agreement_status|unavailable|threshold" docs/benchmark_reporting.md'</automated>
  </verify>
  <done>Operator docs match runtime and CLI semantics for agreement-available and agreement-unavailable runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| protocol report payload -> downstream tooling | Ambiguous field semantics can cause wrong pass/fail interpretation in automation |
| CLI status line -> human triage decisions | Missing agreement-state context can trigger incorrect incident handling |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02-01 | R | reproducibility report semantics | mitigate | Add explicit availability/outcome fields and preserve compatibility keys |
| T-09-02-02 | I | benchmark CLI status output | mitigate | Include agreement state and judge-check context in reproducibility summary |
</threat_model>

<verification>
Run:
- `bash -lc 'rg -n "agreement_available|agreement_status|agreement_passed|gate_summary" src/mobile_world/runtime/protocol/reproducibility.py'`
- `bash -lc 'rg -n "Reproducibility status|agreement" src/mobile_world/core/subcommands/benchmark.py'`
- `bash -lc 'rg -n "agreement_available|agreement_status|unavailable|threshold" docs/benchmark_reporting.md'`
</verification>

<success_criteria>
- Report payload distinguishes agreement availability from pass/fail outcome.
- CLI summary exposes agreement state without requiring JSON inspection.
- Docs define the hardened semantics and fields consistently with runtime behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/09-reproducibility-agreement-gate-hardening/09-02-SUMMARY.md`
</output>
