---
phase: 06-reporting-conformance-reproducibility
plan: 02
type: execute
wave: 1
depends_on: [06-01]
files_modified:
  - src/mobile_world/runtime/protocol/reporting.py
  - src/mobile_world/runtime/protocol/__init__.py
autonomous: true
requirements: [METR-04, TRCE-03]
user_setup: []
must_haves:
  truths:
    - "Cross-framework runs can be aggregated on common task subsets."
    - "Report includes KPI panels for efficiency/cost/latency/reliability/evaluator-quality."
  artifacts:
    - "src/mobile_world/runtime/protocol/reporting.py"
  key_links:
    - "aggregation output contains leaderboard-ready metrics and task matrix."
---

<objective>
Implement cross-framework aggregation and KPI-enriched leaderboard report utilities.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Build run artifact loaders and aggregation logic</name>
  <files>src/mobile_world/runtime/protocol/reporting.py</files>
  <action>Load per-task score/metrics/audit artifacts from run roots and aggregate comparable framework reports over common tasks.</action>
  <verify>rg -n "aggregate_framework_runs|tokens_per_success|cost_per_success|judge_agreement_rate" src/mobile_world/runtime/protocol/reporting.py</verify>
  <done>Aggregator computes comparable KPI summaries and leaderboard rows.</done>
</task>
<task type="auto">
  <name>Task 2: Export reporting helpers</name>
  <files>src/mobile_world/runtime/protocol/__init__.py</files>
  <action>Expose reporting entrypoints for CLI and test imports.</action>
  <verify>rg -n "aggregate_framework_runs|load_task_run_records" src/mobile_world/runtime/protocol/__init__.py</verify>
  <done>Reporting utilities are importable from protocol package root.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/06-reporting-conformance-reproducibility/06-02-SUMMARY.md`
</output>
