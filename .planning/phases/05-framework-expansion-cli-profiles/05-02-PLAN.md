---
phase: 05-framework-expansion-cli-profiles
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/mobile_world/core/subcommands/eval.py
  - src/mobile_world/core/runner.py
  - tests/protocol/test_phase5_framework_profiles.py
autonomous: true
requirements: [INTG-03]
user_setup: []
must_haves:
  truths:
    - "Eval CLI can run by framework profile using direct flags or profile config file."
    - "Framework profile config loading keeps option semantics consistent."
  artifacts:
    - "src/mobile_world/core/subcommands/eval.py"
  key_links:
    - "framework config loading maps to runner adapter-mode args deterministically."
---

<objective>
Add framework-profile eval CLI commands and config loading.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Add framework profile config loader for eval CLI</name>
  <files>src/mobile_world/core/subcommands/eval.py</files>
  <action>Add `--framework-config` JSON loader and normalize framework args for runner calls.</action>
  <verify>rg -n "framework_config|load_framework_config|framework_profile" src/mobile_world/core/subcommands/eval.py</verify>
  <done>Eval CLI supports framework profile config-driven execution.</done>
</task>
<task type="auto">
  <name>Task 2: Add protocol tests for framework profile eval options</name>
  <files>tests/protocol/test_phase5_framework_profiles.py</files>
  <action>Cover framework config parsing and runner argument mapping behavior.</action>
  <verify>rg -n "framework_config|framework_profile|nanobot_fork_path" tests/protocol/test_phase5_framework_profiles.py</verify>
  <done>CLI profile option semantics are regression-tested.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/05-framework-expansion-cli-profiles/05-02-SUMMARY.md`
</output>
