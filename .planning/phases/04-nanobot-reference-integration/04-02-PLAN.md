---
phase: 04-nanobot-reference-integration
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/mobile_world/agents/registry.py
  - src/mobile_world/core/runner.py
  - src/mobile_world/core/subcommands/eval.py
autonomous: true
requirements: [COMP-01]
user_setup: []
must_haves:
  truths:
    - "Built-in agent path remains unchanged when no framework profile is selected."
    - "Runner can execute tasks through registered framework adapter profile when requested."
  artifacts:
    - "src/mobile_world/core/runner.py"
  key_links:
    - "eval CLI passes framework profile to runner without breaking existing flags."
---

<objective>
Add compatibility shim so runner can execute through adapter profiles while preserving legacy path.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Register nanobot profile in adapter registry</name>
  <files>src/mobile_world/agents/registry.py</files>
  <action>Add `nanobot_opengui` profile registration with factory-based adapter creation.</action>
  <verify>rg -n "nanobot_opengui|register_reference_framework_adapters|NanobotOpenGUIAdapter" src/mobile_world/agents/registry.py</verify>
  <done>Reference profile is discoverable via existing registry APIs.</done>
</task>
<task type="auto">
  <name>Task 2: Add runner/CLI adapter execution mode</name>
  <files>src/mobile_world/core/runner.py, src/mobile_world/core/subcommands/eval.py</files>
  <action>Add optional `framework_profile` execution path while preserving default built-in agent behavior.</action>
  <verify>rg -n "framework_profile|create_framework_adapter|FrameworkAdapter" src/mobile_world/core/runner.py src/mobile_world/core/subcommands/eval.py</verify>
  <done>Runner supports adapter mode and legacy mode side-by-side.</done>
</task>
</tasks>

<output>
After completion, create `.planning/phases/04-nanobot-reference-integration/04-02-SUMMARY.md`
</output>
