---
phase: 01-protocol-baseline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mobile_world/runtime/protocol/events.py
  - src/mobile_world/runtime/protocol/normalization.py
  - src/mobile_world/runtime/utils/trajectory_logger.py
  - src/mobile_world/runtime/utils/models.py
autonomous: true
requirements:
  - ADPT-03
  - TRCE-01
must_haves:
  truths:
    - "Built-in actions, tool calls, and score outputs are representable in one canonical event schema."
    - "Every run emits a versioned canonical trajectory artifact without removing legacy output."
  artifacts:
    - "src/mobile_world/runtime/protocol/events.py"
    - "src/mobile_world/runtime/protocol/normalization.py"
    - "src/mobile_world/runtime/utils/trajectory_logger.py"
  key_links:
    - "trajectory_logger calls normalization helpers before canonical JSONL append."
---

<objective>
Define canonical event contracts and dual-write trajectory output so framework runs are comparable under one schema.

Purpose: Land ADPT-03 and TRCE-01 while preserving existing `traj.json` compatibility for current users.
Output: Canonical schema models, runtime normalizers, and logger dual-write integration.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-protocol-baseline/01-RESEARCH.md
@.planning/phases/01-protocol-baseline/01-PATTERNS.md
@src/mobile_world/runtime/utils/models.py
@src/mobile_world/runtime/utils/trajectory_logger.py
@src/mobile_world/runtime/client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement canonical trajectory event models with explicit schema version</name>
  <files>src/mobile_world/runtime/protocol/events.py</files>
  <read_first>src/mobile_world/runtime/utils/models.py, src/mobile_world/runtime/utils/trajectory_logger.py, .planning/phases/01-protocol-baseline/01-PATTERNS.md</read_first>
  <action>Create canonical event models in `events.py` using Pydantic v2 with `CANONICAL_TRAJECTORY_SCHEMA_VERSION = "1.0.0"`. Include concrete models for header, step, tool, and score events with deterministic fields: `run_id`, `task_name`, `event_type`, `step_index` (for step events), `timestamp_ms`, and `payload`. Keep field names stable and JSONL-serializable.</action>
  <verify>
    <automated>python -m compileall src/mobile_world/runtime/protocol/events.py && rg -n "CANONICAL_TRAJECTORY_SCHEMA_VERSION|class CanonicalTrajectoryHeader|class CanonicalStepEvent|class CanonicalScoreEvent" src/mobile_world/runtime/protocol/events.py</automated>
  </verify>
  <acceptance_criteria>
    - Canonical schema has explicit version constant and envelope/event models.
    - Event model set covers action/tool/score data needed for built-in runs.
    - Models can be dumped directly to JSON objects for JSONL writing.
  </acceptance_criteria>
  <done>Canonical event schema is defined and importable for runtime normalization.</done>
</task>

<task type="auto">
  <name>Task 2: Build legacy-to-canonical normalization helpers</name>
  <files>src/mobile_world/runtime/protocol/normalization.py, src/mobile_world/runtime/utils/models.py</files>
  <read_first>src/mobile_world/runtime/protocol/events.py, src/mobile_world/runtime/utils/models.py, src/mobile_world/runtime/client.py</read_first>
  <action>Implement explicit conversion helpers in `normalization.py`: `normalize_step_event`, `normalize_score_event`, and payload helpers that convert `JSONAction`, observation dictionaries, tool call records, and ask-user responses into canonical payloads. Preserve unknown source keys under a deterministic `extensions` map and prevent framework-specific field leakage into top-level schema fields.</action>
  <verify>
    <automated>python -m compileall src/mobile_world/runtime/protocol/normalization.py && rg -n "def normalize_step_event|def normalize_score_event|extensions" src/mobile_world/runtime/protocol/normalization.py</automated>
  </verify>
  <acceptance_criteria>
    - Built-in runtime action/observation structures convert to canonical events without dropped required fields.
    - Optional/unknown source fields are retained under `extensions`.
    - Canonical payload keys remain deterministic across repeated runs.
  </acceptance_criteria>
  <done>Normalization layer is ready for logger integration and cross-framework comparability.</done>
</task>

<task type="auto">
  <name>Task 3: Dual-write canonical JSONL and legacy trajectory artifacts in logger</name>
  <files>src/mobile_world/runtime/utils/trajectory_logger.py</files>
  <read_first>src/mobile_world/runtime/protocol/events.py, src/mobile_world/runtime/protocol/normalization.py, src/mobile_world/runtime/utils/trajectory_logger.py</read_first>
  <action>Update `TrajLogger` to keep writing `traj.json` and additionally write canonical files `traj.canonical.jsonl` and `traj.meta.json`. Emit a header event once per run with `schema_version`, append normalized step events for each step, and append a normalized score event at finalize time. Keep existing screenshot persistence and legacy log data layout unchanged.</action>
  <verify>
    <automated>python -m compileall src/mobile_world/runtime/utils/trajectory_logger.py && rg -n "traj\\.canonical\\.jsonl|traj\\.meta\\.json|schema_version|normalize_step_event" src/mobile_world/runtime/utils/trajectory_logger.py</automated>
  </verify>
  <acceptance_criteria>
    - Logger outputs canonical JSONL + metadata alongside legacy `traj.json`.
    - Canonical files include schema version and required envelope fields.
    - Legacy artifact behavior remains backward-compatible for existing consumers.
  </acceptance_criteria>
  <done>TRCE-01 canonical trajectory persistence is active without breaking legacy output.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Adapter/runtime payloads -> canonical normalizer | Untrusted action/tool payload content crosses into canonical artifact pipeline. |
| Canonical events -> filesystem artifacts | Normalized runtime data is persisted and later consumed by reporting/conformance tools. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | T | `runtime/protocol/normalization.py` | mitigate | Validate event envelope fields with Pydantic models and route non-schema keys into `extensions` instead of allowing top-level schema tampering. |
| T-01-06 | I | `runtime/utils/trajectory_logger.py` | mitigate | Persist only canonical fields and sanitized payload maps; avoid writing raw secret-bearing runtime internals outside allowed payload fields. |
| T-01-07 | D | canonical artifact writer | mitigate | Keep append-only JSONL flow and bounded per-step payload conversion logic to reduce risk of oversized payloads exhausting memory/IO paths. |
| T-01-08 | R | canonical trace events | mitigate | Include deterministic `run_id`, `step_index`, and `timestamp_ms` fields in every event envelope for auditability and replay diagnostics. |
</threat_model>

<verification>
- `python -m compileall src/mobile_world/runtime/protocol src/mobile_world/runtime/utils/trajectory_logger.py`
- `rg -n "CANONICAL_TRAJECTORY_SCHEMA_VERSION|normalize_step_event|traj\\.canonical\\.jsonl" src/mobile_world/runtime/protocol src/mobile_world/runtime/utils/trajectory_logger.py`
</verification>

<success_criteria>
- ADPT-03 covered: runtime output is normalized into canonical action/tool/score events.
- TRCE-01 covered: canonical trajectory artifacts are persisted with explicit schema version.
- Existing `traj.json` consumers remain supported.
</success_criteria>

<output>
After completion, create `.planning/phases/01-protocol-baseline/01-02-SUMMARY.md`.
</output>
