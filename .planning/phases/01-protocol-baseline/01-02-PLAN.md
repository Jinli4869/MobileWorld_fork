---
phase: 01-protocol-baseline
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mobile_world/runtime/protocol/events.py
  - src/mobile_world/runtime/protocol/normalization.py
  - src/mobile_world/runtime/utils/trajectory_logger.py
  - src/mobile_world/runtime/utils/models.py
autonomous: true
requirements:
  - ADPT-03
  - TRCE-01
user_setup: []
must_haves:
  truths:
    - "Action, tool-call, and task-level events are represented in one canonical schema."
    - "Every run writes a versioned canonical trajectory artifact."
  artifacts:
    - "src/mobile_world/runtime/protocol/events.py"
    - "src/mobile_world/runtime/protocol/normalization.py"
    - "src/mobile_world/runtime/utils/trajectory_logger.py"
  key_links:
    - "trajectory_logger converts runtime observations/actions via protocol normalizers before persistence."
---

<objective>
Define canonical action/tool/trace event schemas and wire them into trajectory persistence.

Purpose: Make built-in and future framework adapters emit comparable artifacts.
Output: Canonical event models, normalization utilities, and versioned trajectory metadata output.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-protocol-baseline/01-RESEARCH.md
@src/mobile_world/runtime/utils/models.py
@src/mobile_world/runtime/utils/trajectory_logger.py
@src/mobile_world/runtime/client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce canonical protocol event schema</name>
  <files>src/mobile_world/runtime/protocol/events.py</files>
  <read_first>src/mobile_world/runtime/utils/models.py, src/mobile_world/runtime/utils/trajectory_logger.py</read_first>
  <action>Create canonical event models in `events.py` for run/task/step/action/tool/evaluator envelope records. Include `schema_version`, deterministic timestamp fields, event `type`, and quality/source markers needed for cross-framework normalization. Keep the schema serializable to JSONL and backward-compatible with existing run metadata fields.</action>
  <verify>rg -n "schema_version|class CanonicalStepEvent|class CanonicalTrajectoryHeader" src/mobile_world/runtime/protocol/events.py</verify>
  <acceptance_criteria>
    - Canonical event models cover action, tool call, ask-user response, and score outcome.
    - Schema version is explicit and stored with trajectory metadata.
  </acceptance_criteria>
  <done>Canonical event schema exists and is importable.</done>
</task>

<task type="auto">
  <name>Task 2: Add normalizers from existing runtime payloads to canonical events</name>
  <files>src/mobile_world/runtime/protocol/normalization.py, src/mobile_world/runtime/utils/models.py</files>
  <read_first>src/mobile_world/runtime/client.py, src/mobile_world/runtime/utils/models.py</read_first>
  <action>Implement conversion helpers that transform current `JSONAction`, observation fields (`tool_call`, `ask_user_response`), and score/reason outputs into canonical protocol records. Add defensive handling for missing optional fields and ensure action/tool payloads are preserved without framework-specific shape leakage.</action>
  <verify>rg -n "def normalize_action|def normalize_observation|def normalize_score" src/mobile_world/runtime/protocol/normalization.py</verify>
  <acceptance_criteria>
    - Built-in runtime structures convert into canonical events without losing action/tool semantics.
    - Unknown/optional fields are captured in deterministic extension fields.
  </acceptance_criteria>
  <done>Canonical normalization functions are available for logger integration.</done>
</task>

<task type="auto">
  <name>Task 3: Upgrade trajectory logger to emit versioned canonical artifacts</name>
  <files>src/mobile_world/runtime/utils/trajectory_logger.py</files>
  <read_first>src/mobile_world/runtime/protocol/events.py, src/mobile_world/runtime/protocol/normalization.py</read_first>
  <action>Extend trajectory logging to dual-write existing `traj.json` plus canonical artifact outputs (for example `traj.canonical.jsonl` and metadata file). Ensure each step event includes schema version, step index, normalized action/tool event, and token usage block if present. Preserve existing screenshot persistence and backward compatibility for current viewer/consumers.</action>
  <verify>python -m compileall src/mobile_world/runtime/protocol src/mobile_world/runtime/utils/trajectory_logger.py</verify>
  <acceptance_criteria>
    - Versioned canonical trajectory artifact is persisted for each run.
    - Existing `traj.json` behavior remains intact for legacy consumers.
  </acceptance_criteria>
  <done>Logger emits canonical and legacy artifacts in one run.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m compileall src/mobile_world/runtime/protocol src/mobile_world/runtime/utils/trajectory_logger.py` passes
- [ ] `rg -n "canonical|schema_version|jsonl" src/mobile_world/runtime/utils/trajectory_logger.py src/mobile_world/runtime/protocol` shows canonical path
- [ ] Manual dry check confirms legacy `traj.json` output path still exists in logger code
</verification>

<success_criteria>
- Canonical action/tool/trace schema can represent existing built-in runs.
- Trajectory artifact is explicitly versioned.
- Logger change does not break legacy trajectory consumers.
</success_criteria>

<output>
After completion, create `.planning/phases/01-protocol-baseline/01-02-SUMMARY.md`
</output>
