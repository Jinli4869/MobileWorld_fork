---
phase: 01-protocol-baseline
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/mobile_world/runtime/protocol/validation.py
  - src/mobile_world/core/runner.py
  - src/mobile_world/core/subcommands/eval.py
  - src/mobile_world/core/subcommands/test.py
autonomous: true
requirements:
  - ADPT-04
user_setup: []
must_haves:
  truths:
    - "Runner pre-flight validates adapter and canonical schema contracts before task execution."
    - "Validation failures are actionable and reported before benchmark start."
  artifacts:
    - "src/mobile_world/runtime/protocol/validation.py"
    - "src/mobile_world/core/runner.py"
  key_links:
    - "eval/test subcommands invoke shared protocol validator path before `run_agent_with_evaluation`."
---

<objective>
Integrate protocol validation gates into runner startup and CLI execution paths.

Purpose: Fail fast on contract mismatches and avoid mid-run non-deterministic failures.
Output: Shared validator module plus runner/CLI pre-flight validation integration.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-protocol-baseline/01-01-PLAN.md
@.planning/phases/01-protocol-baseline/01-02-PLAN.md
@src/mobile_world/core/runner.py
@src/mobile_world/core/subcommands/eval.py
@src/mobile_world/core/subcommands/test.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement shared protocol validation module</name>
  <files>src/mobile_world/runtime/protocol/validation.py</files>
  <read_first>src/mobile_world/runtime/protocol/adapter.py, src/mobile_world/runtime/protocol/events.py</read_first>
  <action>Create validation entrypoints that check adapter lifecycle interface completeness, registry registration validity, and canonical schema invariants. Return structured validation reports with machine-readable code + human-readable remediation details, not generic exceptions.</action>
  <verify>rg -n "class ProtocolValidationError|def validate_adapter_contracts|def validate_canonical_schema" src/mobile_world/runtime/protocol/validation.py</verify>
  <acceptance_criteria>
    - Validator reports actionable schema errors before execution.
    - Validation result format can be consumed by CLI and runner logs.
  </acceptance_criteria>
  <done>Protocol validation module is available for startup gate wiring.</done>
</task>

<task type="auto">
  <name>Task 2: Wire validation into runner pre-flight path</name>
  <files>src/mobile_world/core/runner.py</files>
  <read_first>src/mobile_world/core/runner.py, src/mobile_world/runtime/protocol/validation.py</read_first>
  <action>Call protocol validation before environment/task execution begins in `run_agent_with_evaluation`. Ensure failure stops execution early with clear error output, while allowing explicit skip/bypass switch for debugging-only mode if already supported by CLI args.</action>
  <verify>rg -n "validate_.*protocol|ProtocolValidationError|preflight" src/mobile_world/core/runner.py</verify>
  <acceptance_criteria>
    - Runner cannot proceed with invalid protocol contracts by default.
    - Error path points to concrete failing contract section.
  </acceptance_criteria>
  <done>Runner startup gate enforces ADPT-04.</done>
</task>

<task type="auto">
  <name>Task 3: Surface validation gate in eval/test CLI entrypoints</name>
  <files>src/mobile_world/core/subcommands/eval.py, src/mobile_world/core/subcommands/test.py</files>
  <read_first>src/mobile_world/core/subcommands/eval.py, src/mobile_world/core/subcommands/test.py</read_first>
  <action>Expose validation behavior in CLI flow (and optional bypass flag if needed for controlled debugging). Ensure user-facing CLI errors clearly indicate protocol validation failure before benchmark starts.</action>
  <verify>python -m compileall src/mobile_world/core/subcommands/eval.py src/mobile_world/core/subcommands/test.py src/mobile_world/core/runner.py</verify>
  <acceptance_criteria>
    - Eval/test commands surface protocol-gate failures with actionable message.
    - Built-in execution path remains runnable when contracts are valid.
  </acceptance_criteria>
  <done>CLI startup aligns with protocol validation gate.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m compileall src/mobile_world/runtime/protocol src/mobile_world/core/runner.py src/mobile_world/core/subcommands/eval.py src/mobile_world/core/subcommands/test.py` passes
- [ ] `rg -n "validate_.*protocol|ProtocolValidation" src/mobile_world/core/runner.py src/mobile_world/core/subcommands/eval.py src/mobile_world/core/subcommands/test.py` finds startup gate calls
- [ ] Validation error path is deterministic and human-readable
</verification>

<success_criteria>
- Contract validation is integrated before benchmark execution.
- Adapter/schema mismatches fail fast with actionable diagnostics.
- Existing CLI flows remain usable when contracts pass.
</success_criteria>

<output>
After completion, create `.planning/phases/01-protocol-baseline/01-03-SUMMARY.md`
</output>
