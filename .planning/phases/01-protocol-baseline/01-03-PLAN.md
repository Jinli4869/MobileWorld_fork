---
phase: 01-protocol-baseline
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/mobile_world/runtime/protocol/validation.py
  - src/mobile_world/core/runner.py
  - src/mobile_world/core/subcommands/eval.py
  - src/mobile_world/core/subcommands/test.py
autonomous: true
requirements:
  - ADPT-04
must_haves:
  truths:
    - "Protocol contract violations fail before benchmark execution starts."
    - "Validation failures include actionable machine-readable codes and remediation text."
  artifacts:
    - "src/mobile_world/runtime/protocol/validation.py"
    - "src/mobile_world/core/runner.py"
    - "src/mobile_world/core/subcommands/eval.py"
    - "src/mobile_world/core/subcommands/test.py"
  key_links:
    - "eval/test CLI flags propagate skip_protocol_validation into run_agent_with_evaluation preflight."
---

<objective>
Integrate strict preflight protocol validation into runner and CLI entrypoints.

Purpose: Satisfy ADPT-04 by blocking invalid adapter/schema contracts before task runtime side effects begin.
Output: Shared validation module and enforced startup gate in eval/test flows.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-protocol-baseline/01-RESEARCH.md
@.planning/phases/01-protocol-baseline/01-PATTERNS.md
@.planning/phases/01-protocol-baseline/01-01-PLAN.md
@.planning/phases/01-protocol-baseline/01-02-PLAN.md
@src/mobile_world/core/runner.py
@src/mobile_world/core/subcommands/eval.py
@src/mobile_world/core/subcommands/test.py
@src/mobile_world/runtime/protocol/adapter.py
@src/mobile_world/runtime/protocol/events.py
@src/mobile_world/runtime/protocol/registry.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build shared protocol preflight validator with structured issues</name>
  <files>src/mobile_world/runtime/protocol/validation.py</files>
  <read_first>src/mobile_world/runtime/protocol/adapter.py, src/mobile_world/runtime/protocol/events.py, src/mobile_world/runtime/protocol/registry.py, src/mobile_world/runtime/utils/models.py, .planning/phases/01-protocol-baseline/01-PATTERNS.md</read_first>
  <action>Create `ProtocolValidationIssue`, `ProtocolValidationReport`, and `ProtocolValidationError` in `validation.py`. Implement `run_protocol_preflight(strict: bool = True)` to validate adapter registrations and canonical schema invariants with explicit issue codes (`ADAPTER_MISSING`, `ADAPTER_CLASS_INVALID`, `LIFECYCLE_METHOD_MISSING`, `CANONICAL_SCHEMA_INVALID`). Return report in non-strict mode and raise `ProtocolValidationError` in strict mode when issues exist.</action>
  <verify>
    <automated>python -m compileall src/mobile_world/runtime/protocol/validation.py && rg -n "class ProtocolValidationIssue|class ProtocolValidationReport|class ProtocolValidationError|def run_protocol_preflight" src/mobile_world/runtime/protocol/validation.py</automated>
  </verify>
  <acceptance_criteria>
    - Validator returns structured issues with code, severity, location, message, and remediation.
    - Strict mode fails fast on contract errors before any run execution begins.
    - Non-strict mode still reports issues for diagnostics.
  </acceptance_criteria>
  <done>Validation module provides deterministic, actionable protocol diagnostics.</done>
</task>

<task type="auto">
  <name>Task 2: Enforce preflight validation in runner startup path</name>
  <files>src/mobile_world/core/runner.py</files>
  <read_first>src/mobile_world/core/runner.py, src/mobile_world/runtime/protocol/validation.py</read_first>
  <action>Invoke `run_protocol_preflight` at the beginning of `run_agent_with_evaluation` before environment/task execution. Default to strict validation; if `skip_protocol_validation` is set, log explicit warning `"Protocol pre-flight validation explicitly skipped by CLI flag"` and continue only for debug mode. Surface validation summary in logs when preflight passes.</action>
  <verify>
    <automated>python -m compileall src/mobile_world/core/runner.py && rg -n "run_protocol_preflight|Protocol pre-flight validation explicitly skipped by CLI flag|ProtocolValidationError" src/mobile_world/core/runner.py</automated>
  </verify>
  <acceptance_criteria>
    - Runner blocks execution when protocol contracts are invalid and skip flag is not set.
    - Skip behavior is explicit, auditable, and warning-logged.
    - Successful preflight logs checked adapter count.
  </acceptance_criteria>
  <done>Runner startup gate enforces ADPT-04 by default.</done>
</task>

<task type="auto">
  <name>Task 3: Wire validation gate controls into eval and test subcommands</name>
  <files>src/mobile_world/core/subcommands/eval.py, src/mobile_world/core/subcommands/test.py</files>
  <read_first>src/mobile_world/core/subcommands/eval.py, src/mobile_world/core/subcommands/test.py, src/mobile_world/core/runner.py</read_first>
  <action>Add/confirm `--skip-protocol-validation` argument wiring in eval/test command handlers and pass the boolean into runner invocation. Ensure CLI failure output includes validation issue codes so users can fix contracts without inspecting stack traces.</action>
  <verify>
    <automated>python -m compileall src/mobile_world/core/subcommands/eval.py src/mobile_world/core/subcommands/test.py && rg -n "skip_protocol_validation|ProtocolValidation" src/mobile_world/core/subcommands/eval.py src/mobile_world/core/subcommands/test.py</automated>
  </verify>
  <acceptance_criteria>
    - Eval/test command flow exposes protocol validation behavior consistently.
    - Validation failures terminate command with actionable diagnostics.
    - Debug skip path remains explicit and opt-in only.
  </acceptance_criteria>
  <done>CLI entrypoints and runner use one preflight gate behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI flags/config -> runner preflight gate | User-provided flags can alter validation behavior before benchmark execution. |
| Adapter registry contracts -> runtime execution | Invalid adapter implementations can cross into task execution if not blocked preflight. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | E | `core/runner.py` skip flag path | mitigate | Keep strict validation default and require explicit `--skip-protocol-validation`; log skip event loudly for audit visibility. |
| T-01-10 | T | `runtime/protocol/validation.py` | mitigate | Validate adapter class type and required lifecycle methods before runner step loop starts; block on HIGH severity contract defects. |
| T-01-11 | R | eval/test CLI diagnostics | mitigate | Emit deterministic issue codes and remediation text in CLI errors so contract failures are traceable and reproducible. |
| T-01-12 | D | startup validation execution | accept | Validation adds bounded startup overhead only; accepted as low risk because it avoids larger runtime failure costs. |
</threat_model>

<verification>
- `python -m compileall src/mobile_world/runtime/protocol/validation.py src/mobile_world/core/runner.py src/mobile_world/core/subcommands/eval.py src/mobile_world/core/subcommands/test.py`
- `rg -n "run_protocol_preflight|ProtocolValidationIssue|skip_protocol_validation" src/mobile_world/runtime/protocol/validation.py src/mobile_world/core/runner.py src/mobile_world/core/subcommands/eval.py src/mobile_world/core/subcommands/test.py`
</verification>

<success_criteria>
- ADPT-04 covered: contract mismatches are detected and reported before benchmark execution.
- Runner, eval, and test entrypoints share one deterministic validation behavior.
- Validation diagnostics are actionable for users and CI.
</success_criteria>

<output>
After completion, create `.planning/phases/01-protocol-baseline/01-03-SUMMARY.md`.
</output>
