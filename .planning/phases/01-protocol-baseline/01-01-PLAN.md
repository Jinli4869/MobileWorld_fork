---
phase: 01-protocol-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mobile_world/runtime/protocol/__init__.py
  - src/mobile_world/runtime/protocol/adapter.py
  - src/mobile_world/runtime/protocol/registry.py
  - src/mobile_world/agents/registry.py
autonomous: true
requirements:
  - ADPT-01
  - ADPT-02
user_setup: []
must_haves:
  truths:
    - "Framework adapters can be registered/discovered by framework profile name."
    - "Adapter lifecycle defines initialize, step, finalize, and emit_artifacts contracts."
  artifacts:
    - "src/mobile_world/runtime/protocol/adapter.py"
    - "src/mobile_world/runtime/protocol/registry.py"
  key_links:
    - "agents.registry bridge delegates framework adapter discovery to runtime protocol registry."
---

<objective>
Define adapter protocol models and base interfaces for framework-agnostic execution.

Purpose: Establish a stable adapter boundary before touching tool routing, evaluator, or integration logic.
Output: New protocol package for adapter lifecycle contracts plus registry bridge for discovery.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-protocol-baseline/01-RESEARCH.md
@src/mobile_world/agents/base.py
@src/mobile_world/agents/registry.py
@src/mobile_world/runtime/client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add protocol adapter lifecycle models and interface</name>
  <files>src/mobile_world/runtime/protocol/__init__.py, src/mobile_world/runtime/protocol/adapter.py</files>
  <read_first>src/mobile_world/agents/base.py, src/mobile_world/runtime/utils/models.py</read_first>
  <action>Create `runtime/protocol/adapter.py` with typed lifecycle contracts: adapter profile metadata, initialize input/result, step input/result, finalize result, artifact manifest record, and abstract `FrameworkAdapter` interface exposing `initialize`, `step`, `finalize`, `emit_artifacts`. Use Pydantic v2 models for payload/result validation and keep field names deterministic.</action>
  <verify>rg -n "class FrameworkAdapter|class AdapterStepResult|class AdapterProfile" src/mobile_world/runtime/protocol/adapter.py</verify>
  <acceptance_criteria>
    - `src/mobile_world/runtime/protocol/adapter.py` defines lifecycle models for initialize/step/finalize/emit_artifacts.
    - Lifecycle contracts are strongly typed and reusable across frameworks.
  </acceptance_criteria>
  <done>Adapter lifecycle contract module exists and compiles.</done>
</task>

<task type="auto">
  <name>Task 2: Add adapter registry for framework profile lookup</name>
  <files>src/mobile_world/runtime/protocol/registry.py, src/mobile_world/runtime/protocol/__init__.py</files>
  <read_first>src/mobile_world/agents/registry.py</read_first>
  <action>Implement protocol-level adapter registry with register/list/get APIs keyed by framework profile name. Include duplicate registration protection and actionable ValueError messages. Export registry API from `runtime/protocol/__init__.py` for downstream runner integration.</action>
  <verify>rg -n "def register_adapter|def get_adapter|def list_adapters" src/mobile_world/runtime/protocol/registry.py</verify>
  <acceptance_criteria>
    - Registry supports name-based adapter discovery.
    - Error messages identify missing/duplicate adapter profiles.
  </acceptance_criteria>
  <done>Adapter discovery primitives satisfy ADPT-01 registration needs.</done>
</task>

<task type="auto">
  <name>Task 3: Bridge existing agent registry to protocol adapter discovery</name>
  <files>src/mobile_world/agents/registry.py</files>
  <read_first>src/mobile_world/agents/registry.py, src/mobile_world/runtime/protocol/registry.py</read_first>
  <action>Add compatibility bridge helpers in `agents/registry.py` so current CLI paths remain unchanged while new protocol adapter registration/discovery APIs are available. Do not break current `create_agent` flow; augment it with adapter-facing entry points only.</action>
  <verify>python -m compileall src/mobile_world/agents/registry.py src/mobile_world/runtime/protocol</verify>
  <acceptance_criteria>
    - Existing `create_agent` path still exists and is callable.
    - New adapter discovery entry point is available without changing legacy CLI options.
  </acceptance_criteria>
  <done>Legacy and new adapter registration paths coexist.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `python -m compileall src/mobile_world/runtime/protocol src/mobile_world/agents/registry.py` passes
- [ ] `rg -n "FrameworkAdapter|register_adapter|get_adapter" src/mobile_world/runtime/protocol src/mobile_world/agents/registry.py` shows expected symbols
- [ ] Existing agent registry map entries remain intact
</verification>

<success_criteria>
- Adapter lifecycle contract is explicit and typed.
- Framework adapter registration/discovery is available by profile name.
- Legacy agent creation path is preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/01-protocol-baseline/01-01-SUMMARY.md`
</output>
