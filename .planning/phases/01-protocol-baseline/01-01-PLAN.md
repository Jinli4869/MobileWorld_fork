---
phase: 01-protocol-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/mobile_world/runtime/protocol/__init__.py
  - src/mobile_world/runtime/protocol/adapter.py
  - src/mobile_world/runtime/protocol/registry.py
  - src/mobile_world/agents/registry.py
autonomous: true
requirements:
  - ADPT-01
  - ADPT-02
must_haves:
  truths:
    - "Framework adapters can be registered and discovered by profile name."
    - "Adapter lifecycle is explicitly defined as initialize, step, finalize, and emit_artifacts."
  artifacts:
    - "src/mobile_world/runtime/protocol/adapter.py"
    - "src/mobile_world/runtime/protocol/registry.py"
    - "src/mobile_world/agents/registry.py"
  key_links:
    - "agents.registry bridges built-in profiles into runtime.protocol.registry."
---

<objective>
Create the baseline adapter protocol contracts and discovery bridge without breaking existing built-in agent execution paths.

Purpose: Land ADPT-01 and ADPT-02 as strict, typed runtime contracts before canonical trace and validation gating work.
Output: Adapter lifecycle models, protocol registry APIs, and compatibility bridge entrypoints.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-protocol-baseline/01-RESEARCH.md
@.planning/phases/01-protocol-baseline/01-PATTERNS.md
@src/mobile_world/agents/base.py
@src/mobile_world/agents/registry.py
@src/mobile_world/runtime/utils/models.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define adapter lifecycle contract models and interface</name>
  <files>src/mobile_world/runtime/protocol/adapter.py, src/mobile_world/runtime/protocol/__init__.py</files>
  <read_first>src/mobile_world/agents/base.py, src/mobile_world/runtime/utils/models.py, .planning/phases/01-protocol-baseline/01-PATTERNS.md</read_first>
  <action>Create typed protocol models in `adapter.py` with concrete classes `AdapterProfile`, `AdapterInitializeInput`, `AdapterInitializeResult`, `AdapterStepInput`, `AdapterStepResult`, `AdapterFinalizeInput`, `AdapterFinalizeResult`, and `AdapterArtifact`. Add abstract class `FrameworkAdapter` with methods `initialize`, `step`, `finalize`, and `emit_artifacts`. Use Pydantic v2 models for payload/result validation and expose these symbols via `runtime/protocol/__init__.py`.</action>
  <verify>
    <automated>python -m compileall src/mobile_world/runtime/protocol/adapter.py && rg -n "class FrameworkAdapter|class AdapterStepResult|class AdapterProfile" src/mobile_world/runtime/protocol/adapter.py</automated>
  </verify>
  <acceptance_criteria>
    - `adapter.py` exports lifecycle input/output models for initialize, step, finalize, and artifact emission.
    - `FrameworkAdapter` is an abstract interface with all four lifecycle methods.
    - Models are importable from `runtime/protocol/__init__.py`.
  </acceptance_criteria>
  <done>Adapter lifecycle contracts are explicit and type-checked for all framework integrations.</done>
</task>

<task type="auto">
  <name>Task 2: Implement protocol adapter registry APIs for profile discovery</name>
  <files>src/mobile_world/runtime/protocol/registry.py, src/mobile_world/runtime/protocol/__init__.py</files>
  <read_first>src/mobile_world/runtime/protocol/adapter.py, src/mobile_world/agents/registry.py</read_first>
  <action>Implement deterministic registry APIs in `registry.py`: `register_protocol_adapter`, `get_adapter_registration`, `list_adapters`, `has_adapter`, and `clear_adapters`. Require profile-name keys, reject duplicates unless `overwrite=True`, and return actionable errors for missing profiles. Export these APIs from `runtime/protocol/__init__.py`.</action>
  <verify>
    <automated>python -m compileall src/mobile_world/runtime/protocol/registry.py && rg -n "def register_protocol_adapter|def get_adapter_registration|def list_adapters|def has_adapter" src/mobile_world/runtime/protocol/registry.py</automated>
  </verify>
  <acceptance_criteria>
    - Registry supports name-based adapter registration and lookup.
    - Duplicate profile registration fails by default with explicit remediation (`overwrite=True`).
    - Missing profile lookup errors include profile name and remediation text.
  </acceptance_criteria>
  <done>Protocol registry satisfies ADPT-01 discovery requirements for framework profiles.</done>
</task>

<task type="auto">
  <name>Task 3: Bridge built-in agent registry to protocol adapter registration</name>
  <files>src/mobile_world/agents/registry.py</files>
  <read_first>src/mobile_world/agents/registry.py, src/mobile_world/runtime/protocol/registry.py, src/mobile_world/runtime/protocol/adapter.py</read_first>
  <action>Add compatibility bridge helpers in `agents/registry.py`: `register_builtin_protocol_adapters` and `create_framework_adapter`. Register each `AGENT_CONFIGS` entry with framework `mobile_world_builtin` and capabilities `["legacy_agent", "gui_action"]`. Keep existing `create_agent` behavior unchanged, and only add protocol-facing entrypoints for adapter-mode consumers.</action>
  <verify>
    <automated>python -m compileall src/mobile_world/agents/registry.py && rg -n "def register_builtin_protocol_adapters|def create_framework_adapter|mobile_world_builtin" src/mobile_world/agents/registry.py</automated>
  </verify>
  <acceptance_criteria>
    - Existing built-in agent creation flow remains callable without new arguments.
    - Built-in profiles are discoverable through the protocol registry bridge.
    - Adapter creation path is isolated from legacy `create_agent` API behavior.
  </acceptance_criteria>
  <done>Legacy and protocol adapter discovery paths coexist without CLI breakage.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI/runtime config -> adapter registry | Untrusted profile identifiers and registration options cross into executable adapter creation paths. |
| Built-in agent map -> protocol adapter interface | Legacy classes are bridged into new protocol abstraction; type mismatch risks contract bypass. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | S | `runtime/protocol/registry.py` | mitigate | Enforce unique profile names, reject duplicate registration unless `overwrite=True`, and include source metadata in registration records. |
| T-01-02 | E | `agents/registry.py:create_framework_adapter` | mitigate | Only instantiate registered adapter classes and validate they implement `FrameworkAdapter` lifecycle contracts before use. |
| T-01-03 | T | `runtime/protocol/adapter.py` models | mitigate | Use strict Pydantic validation for profile/lifecycle payload fields to block malformed contract inputs entering runtime control flow. |
| T-01-04 | D | adapter factory callbacks | accept | Risk is low in Phase 1 because factories are local code paths; execution remains within benchmark process boundary. |
</threat_model>

<verification>
- `python -m compileall src/mobile_world/runtime/protocol src/mobile_world/agents/registry.py`
- `rg -n "FrameworkAdapter|register_protocol_adapter|create_framework_adapter" src/mobile_world/runtime/protocol src/mobile_world/agents/registry.py`
</verification>

<success_criteria>
- ADPT-01 covered: adapters are registered and discovered by profile.
- ADPT-02 covered: lifecycle contract has initialize/step/finalize/emit_artifacts with typed models.
- Built-in agent CLI compatibility is preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/01-protocol-baseline/01-01-SUMMARY.md`.
</output>
