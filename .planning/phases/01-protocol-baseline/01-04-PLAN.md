---
phase: 01-protocol-baseline
plan: 04
type: execute
wave: 3
depends_on:
  - "01-03"
files_modified:
  - tests/protocol/test_phase1_protocol_baseline.py
  - tests/protocol/test_canonical_trajectory_contract.py
  - tests/protocol/conftest.py
  - pyproject.toml
autonomous: true
requirements:
  - ADPT-04
  - TRCE-01
user_setup: []
must_haves:
  truths:
    - "Built-in agent path still runs with protocol baseline enabled."
    - "Canonical trajectory contract is regression-tested."
  artifacts:
    - "tests/protocol/test_phase1_protocol_baseline.py"
    - "tests/protocol/test_canonical_trajectory_contract.py"
  key_links:
    - "Tests cover runner pre-flight validation and trajectory schema/version output."
---

<objective>
Add baseline regression tests to protect built-in compatibility after protocol introduction.

Purpose: Prevent contract changes from breaking existing users while enabling adapter evolution.
Output: Protocol baseline test suite and canonical trajectory contract assertions.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-protocol-baseline/01-01-PLAN.md
@.planning/phases/01-protocol-baseline/01-02-PLAN.md
@.planning/phases/01-protocol-baseline/01-03-PLAN.md
@src/mobile_world/core/runner.py
@src/mobile_world/runtime/utils/trajectory_logger.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add adapter contract regression tests</name>
  <files>tests/protocol/test_phase1_protocol_baseline.py, tests/protocol/conftest.py</files>
  <read_first>src/mobile_world/runtime/protocol/adapter.py, src/mobile_world/runtime/protocol/validation.py, src/mobile_world/agents/registry.py</read_first>
  <action>Create pytest coverage for adapter registry/discovery and lifecycle validation failures. Include negative tests asserting actionable error messages when adapter lifecycle methods or required fields are missing.</action>
  <verify>rg -n "def test_.*adapter|def test_.*validation" tests/protocol/test_phase1_protocol_baseline.py</verify>
  <acceptance_criteria>
    - Tests verify ADPT-01/02/04 contract behavior at unit level.
    - Failing adapter contracts produce deterministic validation diagnostics.
  </acceptance_criteria>
  <done>Adapter contract baseline tests are implemented.</done>
</task>

<task type="auto">
  <name>Task 2: Add canonical trajectory artifact contract tests</name>
  <files>tests/protocol/test_canonical_trajectory_contract.py</files>
  <read_first>src/mobile_world/runtime/protocol/events.py, src/mobile_world/runtime/protocol/normalization.py, src/mobile_world/runtime/utils/trajectory_logger.py</read_first>
  <action>Write tests asserting canonical trajectory files are emitted with schema version and required step fields for representative built-in actions (GUI action, ask_user, tool_call, score record). Include backward-compatibility assertion that legacy trajectory output remains available.</action>
  <verify>rg -n "def test_.*trajectory|schema_version|legacy" tests/protocol/test_canonical_trajectory_contract.py</verify>
  <acceptance_criteria>
    - Canonical artifact structure is regression-tested.
    - Legacy trajectory format compatibility is covered by tests.
  </acceptance_criteria>
  <done>Trajectory contract tests protect TRCE-01 behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Wire test discovery and run baseline suite</name>
  <files>pyproject.toml</files>
  <read_first>pyproject.toml</read_first>
  <action>If needed, add pytest discovery configuration for `tests/protocol/`. Run targeted test command for new baseline suite and keep output stable for CI consumption.</action>
  <verify>pytest -q tests/protocol/test_phase1_protocol_baseline.py tests/protocol/test_canonical_trajectory_contract.py</verify>
  <acceptance_criteria>
    - New protocol baseline tests are discoverable and executable.
    - Targeted suite passes locally after implementation.
  </acceptance_criteria>
  <done>Phase 1 compatibility guardrails are test-enforced.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pytest -q tests/protocol/test_phase1_protocol_baseline.py tests/protocol/test_canonical_trajectory_contract.py` passes
- [ ] `rg -n "schema_version|canonical|adapter" tests/protocol` confirms test coverage for core contract areas
- [ ] New tests do not require emulator/docker runtime for contract-level assertions
</verification>

<success_criteria>
- Baseline regression tests protect built-in compatibility after contract introduction.
- Canonical trajectory contract is covered by automated tests.
- Startup validation behavior is validated through deterministic test cases.
</success_criteria>

<output>
After completion, create `.planning/phases/01-protocol-baseline/01-04-SUMMARY.md`
</output>
