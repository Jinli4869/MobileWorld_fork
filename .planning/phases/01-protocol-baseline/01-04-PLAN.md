---
phase: 01-protocol-baseline
plan: 04
type: execute
wave: 3
depends_on:
  - "01-03"
files_modified:
  - tests/protocol/conftest.py
  - tests/protocol/test_phase1_protocol_baseline.py
  - tests/protocol/test_canonical_trajectory_contract.py
  - pyproject.toml
autonomous: true
requirements:
  - ADPT-04
  - TRCE-01
must_haves:
  truths:
    - "Protocol preflight and adapter contract behavior are regression-tested."
    - "Canonical trajectory artifacts are regression-tested with legacy compatibility assertions."
  artifacts:
    - "tests/protocol/test_phase1_protocol_baseline.py"
    - "tests/protocol/test_canonical_trajectory_contract.py"
    - "tests/protocol/conftest.py"
  key_links:
    - "Protocol tests assert validation and logger outputs against Phase 1 contracts."
---

<objective>
Add deterministic protocol baseline tests that lock adapter and trajectory contracts after Phase 1 changes.

Purpose: Prevent regressions in ADPT-04 and TRCE-01 while keeping tests runnable in CI/sandbox without emulator dependency.
Output: Protocol contract test suite and optional pytest discovery config updates.
</objective>

<execution_context>
@/Users/jinli/.codex/get-shit-done/workflows/execute-plan.md
@/Users/jinli/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-protocol-baseline/01-PATTERNS.md
@.planning/phases/01-protocol-baseline/01-03-PLAN.md
@src/mobile_world/runtime/protocol/validation.py
@src/mobile_world/runtime/utils/trajectory_logger.py
@tests/protocol/test_phase2_tool_router_policy.py
@tests/protocol/conftest.py
@pyproject.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add adapter contract and preflight validation regression tests</name>
  <files>tests/protocol/test_phase1_protocol_baseline.py, tests/protocol/conftest.py</files>
  <read_first>src/mobile_world/runtime/protocol/adapter.py, src/mobile_world/runtime/protocol/registry.py, src/mobile_world/runtime/protocol/validation.py, src/mobile_world/agents/registry.py, tests/protocol/test_phase2_tool_router_policy.py</read_first>
  <action>Create test cases for: successful profile discovery, duplicate profile registration rejection, missing profile lookup failure, and strict preflight failure for invalid adapter classes/lifecycle methods. Use deterministic assertions on issue codes (`ADAPTER_CLASS_INVALID`, `LIFECYCLE_METHOD_MISSING`) and keep registry isolation fixture in `tests/protocol/conftest.py` so tests are order-independent.</action>
  <verify>
    <automated>rg -n "def test_.*adapter|def test_.*preflight|ADAPTER_CLASS_INVALID|LIFECYCLE_METHOD_MISSING" tests/protocol/test_phase1_protocol_baseline.py</automated>
  </verify>
  <acceptance_criteria>
    - Tests cover ADPT-01/02/04 behaviors through registry and preflight contracts.
    - Failure-mode tests assert deterministic issue codes and remediation-facing diagnostics.
    - Registry state is reset between tests to avoid cross-test contamination.
  </acceptance_criteria>
  <done>Adapter and validation contract regressions are test-protected.</done>
</task>

<task type="auto">
  <name>Task 2: Add canonical trajectory artifact contract tests</name>
  <files>tests/protocol/test_canonical_trajectory_contract.py</files>
  <read_first>src/mobile_world/runtime/protocol/events.py, src/mobile_world/runtime/protocol/normalization.py, src/mobile_world/runtime/utils/trajectory_logger.py</read_first>
  <action>Write tests that execute logger-level trajectory writes and assert: canonical files (`traj.canonical.jsonl`, `traj.meta.json`) exist, header event contains `schema_version`, step events include `run_id` + `step_index`, and score event is present after finalize. Add backward-compatibility assertion that legacy `traj.json` is still written in same run.</action>
  <verify>
    <automated>rg -n "def test_.*canonical|schema_version|traj\\.canonical\\.jsonl|traj\\.json" tests/protocol/test_canonical_trajectory_contract.py</automated>
  </verify>
  <acceptance_criteria>
    - Canonical artifact contract (including header + step + score) is asserted by automated tests.
    - Legacy trajectory compatibility assertion exists in the same suite.
    - Tests avoid emulator/device dependencies and run as pure protocol unit tests.
  </acceptance_criteria>
  <done>TRCE-01 artifact behavior is locked by deterministic regression tests.</done>
</task>

<task type="auto">
  <name>Task 3: Ensure protocol suite discovery and execute targeted tests</name>
  <files>pyproject.toml</files>
  <read_first>pyproject.toml, tests/protocol/test_phase1_protocol_baseline.py, tests/protocol/test_canonical_trajectory_contract.py</read_first>
  <action>If pytest discovery configuration is missing, add explicit inclusion for `tests/protocol`. Run the targeted protocol baseline suite with `uv run --extra dev python -m pytest -q -s tests/protocol/test_phase1_protocol_baseline.py tests/protocol/test_canonical_trajectory_contract.py` and keep command stable for CI reuse.</action>
  <verify>
    <automated>UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q -s tests/protocol/test_phase1_protocol_baseline.py tests/protocol/test_canonical_trajectory_contract.py</automated>
  </verify>
  <acceptance_criteria>
    - New Phase 1 protocol tests are discoverable and executable by one command.
    - Targeted suite passes in local/sandbox environment.
    - No emulator/runtime service boot is required for this suite.
  </acceptance_criteria>
  <done>Phase 1 protocol baseline has repeatable automated regression verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Production protocol code -> test harness assertions | Incorrect or weak assertions can let contract regressions ship unnoticed. |
| Test fixtures -> global adapter registry state | Shared mutable registry can leak between tests and produce false confidence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | T | `tests/protocol/conftest.py` | mitigate | Reset and re-register adapters per test via autouse fixture so registry state cannot be silently modified across cases. |
| T-01-14 | R | validation regression tests | mitigate | Assert exact issue codes/messages for failing preflight scenarios to keep diagnostics contract auditable. |
| T-01-15 | D | protocol test runtime | mitigate | Keep tests emulator-independent and unit-level to avoid infrastructure-induced flakiness that can hide real regressions. |
| T-01-16 | I | artifact contract tests | accept | Test data is synthetic and non-sensitive; disclosure risk is low and confined to local test outputs. |
</threat_model>

<verification>
- `UV_CACHE_DIR=/tmp/.uv-cache uv run --extra dev python -m pytest -q -s tests/protocol/test_phase1_protocol_baseline.py tests/protocol/test_canonical_trajectory_contract.py`
- `rg -n "schema_version|ADAPTER_CLASS_INVALID|LIFECYCLE_METHOD_MISSING|traj\\.canonical\\.jsonl" tests/protocol`
</verification>

<success_criteria>
- ADPT-04 validation behavior is covered by deterministic automated tests.
- TRCE-01 canonical trajectory contract is covered by automated tests.
- Test suite remains CI-friendly and emulator-independent.
</success_criteria>

<output>
After completion, create `.planning/phases/01-protocol-baseline/01-04-SUMMARY.md`.
</output>
